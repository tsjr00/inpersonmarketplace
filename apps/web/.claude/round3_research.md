# Round-3 research notes (2026-08-27) — recovery points, written as read

## R3-3 Location/event invitation mechanics — READ (cite lines)
- `api/vendor/markets/route.ts:467-483` → `pendingInvitations` = market_vendors WHERE response_status='invited' AND approved=false. **NO market_type filter** (markets join already selects market_type :478).
- Event matching `lib/events/event-actions.ts:374-379` inserts `{market_id, vendor_profile_id, response_status:'invited', invited_at}` → approved defaults false → **every event invite ALSO renders as a "Pending Market Invitation" on My Locations** (`vendor/markets/page.tsx:163`, `PendingMarketInvitations.tsx:187` labels it "Event"). Root cause of the duplication. Confirmed.
- Manager invite flow (`api/market-manager/[marketId]/vendor-invitations/route.ts:247-249`) inserts the same shape for traditional markets — the list was built for that flow (NEW-8).
- Location respond route `api/vendor/markets/[id]/respond/route.ts` gate = response_status='invited' AND approved=false (:~110-125); on accept sets response_status='accepted' **+ approved=true** (:~135-140). **NO market_type check** → accepting an EVENT through the location card:
  1. bypasses the event route's requirements (`api/vendor/events/[marketId]/respond/route.ts`): menu selection (FT 4–7 items), capacity caps (event_max_orders_total/per_wave), agreement acceptance snapshot, same-date event conflict check, wave recalc, admin+organizer notifications, event_vendor_listings + listing_markets upserts.
  2. response_status='accepted' makes the vendor ATTENDING per mig 234 (`applied/20260816_234…sql:218-224`: accepted + not benched [+ fee paid]) with NO menu and NULL caps (recalculate_wave_capacity RAISEs on missing cap per the event route comment).
  3. approved=true fires `trigger_auto_create_vendor_schedules` (snapshot :2658-2662; body mig 210 `:76-105`): FM only (FT skipped :88) → inserts vms rows for every active market_schedules of the EVENT market → `check_vendor_schedule_conflict` (mig 066) can RAISE "Schedule conflict: overlapping times with …" → the whole accept fails for a single-truck FM vendor with an overlapping weekday schedule.
- Event accept route NEVER sets approved=true. Consumers of approved=true that events might hit: `lib/markets/checkin-eligibility.ts:95-100` (comment says "covers traditional + events" — TO READ), `lib/surveys/lazy-generate.ts:62-80` (surveys need approved + active schedule → events never surveyed; fine, market-day feature), `park-week-schedule.ts:222` (park-scoped). mig 217: approved = MANAGER's decision; response_status = VENDOR's answer (opposite directions — don't overload).
- Bottom list on My Locations = `EventMarketsSection.tsx` fed by `eventMarkets` (`api/vendor/markets/route.ts:226-259`: market_type='event' AND event_end_date>=today, responseStatus from mv row; pill Attending/Declined/Invited/Not attending :231-246). Cards carry **Set Schedule / Manage Schedule** buttons (:250-270) → MarketScheduleSelector → `api/vendor/markets/[id]/schedules` PUT → FT-only cross-market conflict check (:232-287, ERR_SCHEDULE_CONFLICT "Schedule conflict: you're already at "X" on Saturdays…"). A vms row does NOTHING for event selling (mig 234/235: event branch reads mv only; flow-integrity :1679 "event attendance is market_vendors.response_status, not a vms row"). ⇒ the event card's Set Schedule button is dead weight that can only produce a false "conflict".
- Two candidate sources for the owner's "schedule conflict notice": (a) FT vendor pressed Set Schedule on an event card (schedules route check) — most likely for truck2 (FT); (b) FM vendor accepted an event via the location card → trigger RAISE. UNVERIFIED which; both are real defects.
- Existing event availability check = event route "Conflict detection" (other ACCEPTED EVENTS overlapping dates; single-truck 409, multi-truck note). Nothing checks traditional schedules / park bookings / pre-orders; matching (`event-actions.ts:211-220`) filters status/event_approved only.

## R3-2 — `OnboardingChecklist.tsx:100-116` banner when `status.canPublishListings`; rendered `vendor/dashboard/page.tsx:400-404`. Dashboard already queries published listings (out-of-stock/low-stock head counts :199-215) — add a published-count head query → prop `hasPublishedListings` → checklist returns null when true.

## R3-1 — copy sites: PendingSurveysCard.tsx:49 (+ tile empty copy), SurveyForm.tsx:108 (thanks), vendor/surveys/page.tsx:105-108, survey/[token]/page.tsx:112 (BUYER page — audience differs; leave "manager/funders" there? buyers don't care either → soften). Vendor categories (`lib/surveys/types.ts` CATEGORY_DEFINITIONS): foot_traffic ✓, sales ✓, market_organization "Setup, layout, signage, scheduling", manager_support "Communication and on-site help from the manager". Owner's list: cleanliness, navigation, parking/spot access, signage, foot traffic. No columns for cleanliness / access → would need a migration (flag; not in approved scope). Labels are static (no vertical) — SurveyForm has `vertical` prop.

## R3-5 — OrderCard.tsx:292 `whiteSpace:'nowrap'`; :530 width 60 thumb; page :418 containers.xl, :467 grid minmax(110px). No custom viewport meta (Next default). TO READ the regions.

## BUILT 2026-08-27 (gates: tsc clean · eslint 0 errors · vitest 2042/2042 → +2 guards)
- R3-2: `vendor/dashboard/page.tsx` +published head-count query (parallel, appended to Promise.all) → `hasPublishedListings` prop; `OnboardingChecklist.tsx` returns null when canPublishListings && hasPublishedListings. Effect: banner shows only between "can publish" and first published listing. Non-approved vendors unchanged (query only runs for approved).
- R3-5: `OrderCard.tsx` header name row wraps (flexWrap + minWidth:0 + overflowWrap), Loyalty chip lost `whiteSpace:nowrap`; `vendor/orders/page.tsx` container `overflowX:'hidden'`. Cause was the chip added 2026-08-25 on a non-wrapping row (high confidence; owner to confirm on iPhone).
- R3-3: `api/vendor/markets/route.ts` pendingInvitations drops `market_type==='event'` rows (JS filter after join); `api/vendor/markets/[id]/respond/route.ts` selects market_type and returns 409 `ERR_EVENT_INVITATION` + actionUrl to the Vendor Event Page for event rows. Staging load-bearing query (event rows with approved=true) = 0 rows → nothing currently depends on the bypass. New flow-integrity describe "Event invitations never surface as location invitations" (2 tests). Map: 11_Vendor_Orders lines + INDEX stamp.
- R3-1: `lib/surveys/types.ts` vendor labels/descriptions (columns unchanged: market_organization → "Layout, signage & getting around"; manager_support → "Site access & on-site support" incl. parking/load-in/cleanliness); overall desc noun-free. Copy: SurveyForm thanks + placeholders, PendingSurveysCard title/body, vendor/surveys + buyer/surveys pages, vendor/survey/[surveyId] + survey/[token] closed states, notification messages (survey_request_vendor/buyer), email.ts (vertical-aware nouns + sender brand FT = "Food Truck'n"; cron passes `vertical`). Manager's SurveyResultsCard shows the new labels automatically (reads CATEGORY_DEFINITIONS).
- NOT done (flagged): cleanliness + parking as their own ratings = migration; event vendors accepted via the proper event flow have approved=false → excluded from check-in eligibility source (a) (`checkin-eligibility.ts:95-100`) — latent, separate decision.
