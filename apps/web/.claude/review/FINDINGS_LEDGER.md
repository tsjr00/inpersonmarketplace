# Findings Ledger — pre-re-release review series

One accumulating record. Grouped by severity. `Status`: open / verifying / fixing / fixed / rejected / wontfix.
Reviewer = Fable (per-slice); findings taken as-is, verified at fix-time against the target lines.

---

## Slice 1 — Checkout & payments (Fable, 2026-07-12)

Coverage: read cart/{route,items,items/[id],validate}, checkout/{session,success,external,payment-methods}, lib/stripe/{payments,webhooks,connect,reconcile,session-status,config,market-box-payout}, lib/pricing, lib/payments/{vendor-fees,cancellation-fees,tip-math}, lib/inventory, + cross-checked checkout/page.tsx + cart RLS + SCHEMA_SNAPSHOT. No P0s. Clean: pricing/cancellation/tip pure fns; Stripe idempotency keys deterministic; transfer-after-DB-failure guarded; MB double-payout guarded; cart-item ownership enforced. The 5 P1s were Opus-verified against live code (all confirmed).

### P1

| ID | Cat | Conf | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|---|
| CHK-1 | money-path | Confirmed | checkout/session/route.ts:110-141; stripe/webhooks.ts:172-175; checkout/success/route.ts:73-77 | Expired-order cleanup cancels + restores inventory but never `sessions.expire()`; paid-flip has no `status='pending'` guard → buyer pays a cancelled order in a stale tab; charged for dead order + oversold. | In cleanup call `sessions.expire()` before cancel; add `.eq('status','pending')` to both paid-flips; route a paid-but-cancelled order to the existing auto-refund path (webhooks.ts:1395-1404 pattern). | M | **partial** |

> **CHK-1 partial (2026-07-12):** ROOT fix done — session-expire in the cleanup loop (`checkout/session/route.ts:110-127`): abandoned Stripe session is expired before the order is cancelled, so it can't be paid afterward; expire-throws (already-complete) skips cancelling a possibly-paid order. **DEFERRED (own reviewed change):** the `status='pending'` guard on the paid-flip + refund-routing in `webhooks.ts`/`checkout/success.ts` — needs a careful 3-way status branch (pending→flip / paid→idempotent backfill / cancelled→refund+stop) so it doesn't break the load-bearing webhook resend backfill. **ALSO:** verify `cron/expire-orders` cancels pending orders without expiring their sessions (same gap) — check in the crons slice.
| CHK-2 | money-path | Confirmed | checkout/session/route.ts:171-227 (match 194-203) | Dup-order match compares listing order_items only (ignores tip + market-box). MB-only carts have empty order_items → `[]==[]` matches ANY recent MB pending → reuses old session. (a) tip change silently dropped; (b) MB swap → charged/subscribed to wrong box. | Narrowed reuse to pure listing carts (no MB, no tip, non-empty match) — session/route.ts:203. | S | **fixed** |
| CHK-3 | contract-break | Confirmed | cart/validate/route.ts:28-57 (esp :28,:48); RLS 20260130_007:574-577 | GET filters `cart_items` by `user_id` — a column that doesn't exist (RLS proves ownership is `cart_id→carts.user_id`); query errors, error discarded → route always returns `valid:true`. Mixed-market-type/same-market/cutoff pre-checkout guard permanently fail-open. | Dropped non-existent `user_id` filter (RLS scopes ownership); fail-closed on query error — validate/route.ts:28-52 (option A). | S | **fixed** |
| CHK-4 | correctness | Confirmed | checkout/session/route.ts:268-270 | `listings.length !== items.length` rejects a cart with the same listing on two pickup dates (`.in('id',ids)` returns 1 row for 2 items) → `ERR_CHECKOUT_001`, legit cart can't check out. | Compare against unique IDs: `listings.length !== new Set(listingIds).size` — session/route.ts:268. | S | **fixed** |
| CHK-5 | security | Confirmed | checkout/external/route.ts:25-56; flag constants.ts:12 | "External payments INACTIVE" lock is client-side only; server route never checks `EXTERNAL_PAYMENTS_ENABLED` → direct POST creates a live external order, decrements inventory, notifies vendor. | Added `if (!EXTERNAL_PAYMENTS_ENABLED) return 403` at top of route — external/route.ts:35-37. | S | **fixed** |

### P2 (Fable-reported; verify at fix-time)

| ID | Cat | Conf | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|---|
| CHK-6 | money-path | Confirmed(F) | checkout/success:240,257; webhooks:252,266; session:709-724,654 | MB failure/at-capacity auto-refund refunds `mbItem.priceCents` (pre-fee) but buyer paid `round(price×1.065)`+fee share → buyer shorted ~6.5%. | Refund the fee-inclusive charged amount (store in metadata) or full-PI refund for MB-only. | S | **fixed** (all 4 unified-path refund sites now `round(priceCents×(1+FEES.buyerFeePercent/100))` — matches the charged line item session:682, works for in-flight sessions; flat service fee stays order-level [not per-item] by design; standalone path was already fee-inclusive) |
| CHK-7 | data-integrity | Confirmed(F) | checkout/session:778-798; lib/inventory:57-77; cleanup:120-137 | Inventory decrement per-item mid-loop, no rollback; `restoreOrderInventory` restores ALL non-cancelled items incl. ones never decremented → oversell. Also concurrent cleanup can double-restore (restore before `cancelled_at` set). | On decrement fail, cancel + restore only already-decremented items; set `cancelled_at` first via guarded update, restore only if guard matched. | M | **fixed** (2026-07-18: NEW `cancelOrderItemsAndRestoreGuarded` in lib/inventory.ts — guarded claim first, restore ONLY claimed rows, pre-cancel status drives FT restore rules; wired into checkout cleanup (+ order flip now guarded `.eq status pending` → Rule A allowlist entry removed per rot-check). Decrement loop now tracks successes; mid-loop failure → expire session + guarded cancel + restore only tracked + rethrow. CRN-5 (cron Phases 2/3) = same helper, lands in Batch 4) |
| CHK-8 | correctness | Confirmed | stripe/webhooks:772-777 (contrast :729) | `handleInvoicePaymentFailed` checks only `subscriptionType==='vendor'`, omits `'food_truck_vendor'` → FT vendor renewal failures never set `past_due`. | Added `|| food_truck_vendor` to the failed handler. | S | **fixed** |
| CHK-9 | correctness | Confirmed | stripe/webhooks:730-736,773-777 | Invoice renewal handlers update vendor_profiles by `user_id` only (no `vertical_id`) → multi-vertical vendor's renewal writes onto BOTH profiles → lapsed vertical stays premium indefinitely. | Both handlers now read `metadata.vertical` + `.eq('vertical_id', vertical)` (refuse+log if missing, mirrors A3). | S | **fixed** |
| CHK-10 | contract-break | Confirmed(F) | stripe/webhooks:166-292 vs checkout/success:312-473 | Regular-order notifications (`new_paid_order`, `order_placed`) + cart-clear live only in success route; webhook backup path does neither → buyer closes tab after paying → vendor not notified + cart not cleared. | Replicate the (idempotent) notification block + cart-clear into `handleCheckoutComplete`. | M | open |
| CHK-11 | efficiency | Confirmed(F) | checkout/session:438-445 | Calls `is_listing_accepting_orders` per listing while batched `get_listings_accepting_status` exists (used by cart/validate). | One batched call; per-listing detail only for the failing one. | S | N RPC/checkout→1 | **fixed** (2026-07-18: batched `get_listings_accepting_status(p_listing_ids)`, error-tolerant like the old per-row path; `get_listing_market_availability` detail call still only for a failing listing) |

### P3 (Fable-reported; verify at fix-time)

| ID | Cat | Conf | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|---|
| CHK-12 | efficiency | Confirmed(F) | checkout/session:448-452 vs :236-249 | Second `listings` query for `id,quantity` already fetched in the parallel batch. | Add `quantity` to the first select; drop the second. | S (2 queries→1) | **fixed** (2026-07-18: `quantity` added to the parallel-batch select + Listing interface; inventoryMap built from it; second query deleted) |
| CHK-13 | correctness | Confirmed(F) | stripe/webhooks:24-40 | `wasNotificationSent` dedups on user+type/24h, ignores `referenceId` → 2nd legit same-type event/day suppressed (2 payouts, 2 refunds). | Add `referenceId` to the dedup query. | S | **fixed** (query += `.contains('data',{dedupRef})`; all 7 paired sendNotification calls store `dedupRef`; `dedupRef?: string` added to NotificationTemplateData [field, not a type — tripwire unaffected]. Pre-deploy notifications lack dedupRef → one possible dup right after deploy, acceptable) |
| CHK-14 | money-path | High(F) | checkout/session:566-575 | Mixed listing+MB cart: vendor-tip cap computed on subtotal incl. MB, tip distributed only across listings → over-allocates tip to listing vendors, under-records `tip_on_platform_fee_cents`. | Compute the cap on listing-only subtotal. | S | **fixed** (cap now on listingSubtotalCents = Σ orderItems.subtotal_cents; verified pricingItems includes MB at session:536-548, so the old cap WAS MB-inclusive; MB-only cart w/ tip → cap 0 → whole tip to tip_on_platform_fee_cents, honest since no order_items exist to distribute to) |
| CHK-15 | money-path | Confirmed(F) | checkout/session:539-543 vs :566; pricing:134-139 | `orders.platform_fee_cents` uses std 6.5% while per-item payout honors `vendor_fee_override_percent` → order-level fee bookkeeping overstates revenue for discounted vendors (reporting only; transfers correct). | Sum per-item fee components for order-level `platform_fee_cents`. | S | **fixed** (2026-07-18: order-level % portion = Σ order_items.platform_fee_cents (override-aware) + MB % at std rates (matches MB payout math — market-box-payout.ts:85 has no override concept, verified) + flats + small-order fee. ADM-2 report helper doesn't read this column — no report interaction) |
| CHK-16 | correctness | Confirmed(F) | checkout/session:64,86,231 | `items` destructured with no default, used unchecked → body without `items` throws TypeError → traced 500 instead of 400. | Default `items=[]` + empty/malformed-cart 400 (session:64,72). | S | **fixed** |
## Slice 2 — Vendor orders lifecycle (Fable, 2026-07-12)

Coverage: vendor/orders/route.ts + all [id] subroutes (confirm/ready/fulfill/reject/cancel-nonpayment/payment-not-received/confirm-external-payment/confirm-cash-complete[410]/confirm-handoff[dormant]/resolve-issue); lib/orders/{status-transitions,checkout-helpers}, lib/inventory, payments/{tip-math,vendor-fees}, stripe/payments payout/refund helpers; buyer/orders/[id]/confirm (payout counterpart); vendor_payouts/vendor_fee_balance RLS history. Clean: authz/IDOR (every active route checks ownership), reject double-cancel guard, insert-before-transfer + partial-unique index + deterministic idempotency keys, no N+1s. Root cause of the P0s: payouts gate on confirmation timestamps, not payment/item state.

### P0 (Opus-verified against live code)
| ID | Cat | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| VOR-1 | money-path/security | fulfill/route.ts:313-333 (+buyer/confirm:202-208) | Fulfill transfers vendor payout with NO paid-order/succeeded-payment gate; when no succeeded payment exists it transfers WITHOUT source_transaction = from platform balance. Buyer never pays (orders created pending pre-payment) → confirm→ready→ack→fulfill pays out real money for nothing. Exploitable w/ buyer+vendor accounts. | Before ANY transfer (fulfill + buyer-confirm edge path): require orders.status IN ('paid','completed') OR a succeeded payments row; hard-fail else. | M | **fixed** (gate before BOTH fulfill branches — incl. vendor-fulfills-first, so unpaid goods handoff + expiry-escaping 'fulfilled' items also blocked — fulfill:~96-120; same gate in buyer-confirm edge path before any write; scoped `!isExternalPayment && !isCompanyPaid`; short-circuits on orders.status paid/completed [verified the only post-payment statuses], payments-row fallback; new ERR_ORDER_007 cataloged) |
| VOR-2 | money-path/data-integrity | fulfill/route.ts:84,459-465 | Fulfill else-branch flips item→'fulfilled' unconditionally (no status/cancelled_at guard); a rejected+refunded item can be fulfilled + paid → buyer keeps refund AND vendor paid. | Make fulfill status writes conditional `.in('status',['confirmed','ready']).is('cancelled_at',null)` + rowcount check; add cancelled_at guard to buyer-confirm. | S | **fixed** (normal branch: `.eq('status','ready')` [tightened — 'confirmed'-with-buyer-ack unreachable per confirm:81] + `.is('cancelled_at',null)` + rowcount→ERR_ORDER_004; else branch: `.in('status',['pending','confirmed','ready'])` [pending→fulfilled kept, VOR-11] + same guards; buyer-confirm edge update: `.eq('status','fulfilled')`+cancelled_at+rowcount. NOTE: refund webhook sets status='refunded' WITHOUT cancelled_at [webhooks:1015], so status-list+cancelled_at BOTH required) |
| VOR-3 | money-path/contract-break | buyer/orders/[id]/confirm/route.ts:173,191,251 | Buyer-confirm payout uses the BUYER supabase client for vendor_payouts (RLS default-deny, no INSERT policy) → payout insert silently fails, transfer fires untracked (no source_transaction), OR when Stripe not ready vendor never paid + no trace. fulfill deliberately uses serviceClient. | Use createServiceClient() for all vendor_payouts reads/writes + fee-balance read in buyer-confirm; non-23505 insert error fatal before transfer. | S | **fixed** (COMPLETE: serviceClient for vendor_payouts [prior commit] + fee-balance read now serviceClient [vendor_fee_balance RLS is vendor/admin-only, mig 046:90-97 — buyer client read 0 rows → deduction silently skipped] + non-23505 payout-insert error now fatal via traced.fromSupabase before transfer) |

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| VOR-4 | money-path | buyer/confirm:145 (select :55-62) | Buyer-confirm pays tip on FULL tip_amount, never subtracts tip_on_platform_fee_cents (not even selected), unlike fulfill (:246-247) → overpays vendor the platform tip share (violates decisions.md 2026-02-20). | Select tip_on_platform_fee_cents + calculateVendorTip() before calculateTipShare, mirroring fulfill. | S | **fixed** (select += tip_on_platform_fee_cents; vendorTipCents = tip_amount − tip_on_platform_fee_cents before calculateTipShare, mirroring fulfill) |
| VOR-5 | money-path (policy) | reject/route.ts:114-117 (vs checkout:587) | Full-order rejection refunds only subtotal+6.5%+flat — tip + small-order fee never refunded even when every item rejected. Same in resolve-issue:149-151. | Product decision, then refund tip + small-order fee on full rejection (per-item tip share on partial). | M | **fixed** (DECISION 2026-07-13 = Option B: when the LAST live item is cancelled, additionally refund tip_amount + recomputed small-order fee via deterministic-key createRefund — both reject + resolve-issue; partial rejections unchanged; per-item tip-share refinement deferred. Adjacent sites cron-Phase-1 + buyer-cancel = VOR-16, open) |
| VOR-6 | money-path (policy) | resolve-issue/route.ts:140-201 | issue_refund on a fulfilled item refunds buyer full amount but never reverses/ledger-debits the already-executed vendor transfer → platform eats it; colluding vendor can donate refunds at platform cost. | Decide policy; transfer reversal or vendor_fee_ledger debit when refunded item has a processing/paid payout. | M | **fixed** (DECISION 2026-07-13 = Option B: paid/processing/pending payout → vendor_fee_ledger debit of payout.amount_cents [tip share included], DB-idempotent via mig-155 unique idx; failed/pending_stripe_setup payouts → CANCELLED so Phase 5 retry can't pay a refunded item [retry hole closed]; disclosure in dialog copy + route message + toast. PLUS payout stack per same-day decision: connect.ts delay_days=3 + $50 min balance via rawRequest [best-effort, logError on failure] + vendor-facing copy on the Stripe setup page) |

### P2/P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| VOR-7 | money-path | confirm-handoff/route.ts:245,265-271 | Dormant-but-live endpoint inserts vendor_payouts w/ user client (RLS-fails, VOR-3) then transfers WITHOUT source_transaction (Session-74 incident pattern). Not UI-reachable but a deployed authed route. | 410 stub like confirm-cash-complete until re-activated, or port fulfill's serviceClient+chargeId. | S | open |
| VOR-8 | money-path/data-integrity | fulfill/route.ts:226-241 | Fee auto-deduction is read-compute-deduct with no atomic claim → two near-simultaneous fulfills both deduct full balance → over-deducted, ledger negative. | Atomic claim RPC (decrement + return granted) or per-vendor advisory lock. | M | **fixed** (2026-07-18, mig 197 `claim_vendor_fee_deduction`: FOR UPDATE on vendor_fee_balance row + credit insert in one txn, replay-safe per order item via new partial unique idx `uq_vendor_fee_ledger_credit_item`. SCOPE EXTENDED at fix time: the same read-compute-deduct lived in buyer-confirm:193-214 and confirm-handoff:195-215 — all THREE payout routes now claim-first via `claimVendorFeeDeduction` (vendor-fees.ts). Pre-mig-safe: RPC error → deduct 0 + logError ERR_FEE_002, fee stays on ledger) |
| VOR-9 | money-path | fulfill/route.ts:336-348 | After a successful transfer that withheld feeDeductionCents, a recordFeeCredit failure is swallowed (crumb only) → same fee deducted again next payout; nothing in error_logs. | On credit failure post-deduction, logError(TracedError) (reject:176-180 pattern) or retried service insert. | S | **fixed** (2026-07-18, structurally: claim-first (mig 197) records the credit BEFORE the transfer — no post-transfer ledger write remains to swallow. Also killed the WORSE variant found at fix time: buyer-confirm's transfer-failure path never wrote the credit at all → guaranteed double-deduct after Phase 5 retry. Bonus same-class fix: confirm-handoff's non-23505 payout-insert failure now fatal (was: continue to bare untracked transfer — VOR-15 mirror, user-approved H4)) |
| VOR-10 | correctness/money | reject/route.ts:156-182 (resolve-issue:177-200) | If no succeeded payments row, reject silently skips the Stripe refund (item cancelled + refund_amount recorded, buyer never refunded, nothing logged); reachable via async pay methods / webhook lag. `.single()` on 0 rows errors + ignored. | When payment_method==='stripe' (shouldCallStripeRefund) + no succeeded row: logError instead of skip; use .maybeSingle(). | S | **fixed** (2026-07-18: both routes — payment_method+payment_model added to order joins; .maybeSingle(); Stripe-paid + no succeeded row → logError ERR_REFUND_001 "needs manual processing" (refund behavior when a row EXISTS unchanged); company_paid exempt per the documented backlog assumptions (organizer settlement, no payments row by design); resolve-issue's whole Stripe block now also gated !company_paid) |
| VOR-11 | contract-break | lib/orders/status-transitions.ts:88-95 | Spec module imported by nothing but tests; live routes contradict it (pending→fulfilled, fulfilled→cancelled). Fixing VOR-2 via these helpers would change resolve-issue behavior. | Decide per-path truth; update spec docs or wire isValidItemTransition into routes. | S | open |
| VOR-12 | efficiency | resolve-issue/route.ts:117-131 | Admin dispute notify loops sendNotification per admin (≤5 seq) vs sendNotificationBatch. | Swap to batch. | S | open |
| VOR-13 | efficiency | fulfill/route.ts:226-271 | Fee-balance read + tip-count + existingPayout are 3 sequential awaits before payout insert. | Promise.all — only while already editing for VOR-1/2. | S | **fixed** (2026-07-18: tip-count + existingPayout → Promise.all; the fee read became the mig-197 claim, which must stay sequential after the existingPayout early-return — an already-paid item must never claim) |
| VOR-14 | money-path | buyer/confirm (edge branch, found during VOR-1 fix 2026-07-12) | buyer-confirm has NO company-paid branch — a `payment_model='company_paid'` order reaching the vendor-fulfilled-first edge attempts a real Stripe transfer instead of organizer settlement (fulfill handles this correctly at its isCompanyPaid branch). VOR-1 gate now computes isCompanyPaid and exempts it from the paid-gate, but the payout path below still runs. Pre-existing. | Add a company-paid early-return mirroring fulfill's (atomic-complete + notify, no transfer). | S | **deferred** (company-paid backlog package 2026-07-14 — unreachable while EVT-1 keeps the feature dead; MUST land with that package) |
| VOR-16 | money-path | cron expire-orders Phase 1 (per-item refund :204-217 region) + buyer/orders/[id]/cancel (found during VOR-5 design 2026-07-13) | Same tip/small-order-fee leak VOR-5 fixed, at the two OTHER order-killing sites: when cron expiry or buyer-cancel cancels an order's LAST item, the order-level tip + small-order fee are never refunded. Same fix pattern as VOR-5(B) — needs user's nod to extend the decision to these sites. | Port the VOR-5(B) block (recompute small fee + deterministic-key refund) into both sites' zero-remaining branches. | S each | **fixed** (user extended VOR-5B 2026-07-14. Cron Phase 1: full port — tip + recomputed small fee, `${orderId}-order-fees` key, payment from the F6 batch prefetch. Buyer-cancel: **TIP ONLY** — its per-item refunds already include the prorated small-order-fee share via cancellation-fees.ts:72-73, so the full port would have double-refunded the fee. Known edge: mixed cancel paths on one order can make reject's recomputed full small fee overlap buyer-cancel's per-item shares by a few cents — Stripe same-key idempotency prevents double-pay; accepted) |
| VOR-17 | money-path | buyer/orders/[id]/cancel/route.ts:265 (found by money-structure Rule D, 2026-07-13) | Cancellation-fee vendor-share transfer (F7) passes NO sourceTransaction → draws on platform balance (Session-74 pattern); charge is only partially refunded so the source is available. | Add the chargeId lookup + sourceTransaction (fulfill pattern). | S | **fixed** (2026-07-14: chargeId from the route's existing succeeded-payment row → sourceTransaction spread; Rule D allowlist entry removed) |
| VOR-18 | money-path | buyer/orders/[id]/confirm/route.ts:257 (found by money-structure Rule D, 2026-07-13) | Buyer-confirm edge payout transfer passes NO sourceTransaction — the VOR-1 gate added there fetches the succeeded payment but never feeds its charge to the transfer (fulfill does). | Reuse the gate's payment row → getChargeIdFromPaymentIntent → sourceTransaction. | S | **fixed** (2026-07-14: fresh succeeded-payment select inside the transfer try [the VOR-1 gate short-circuits on order status and never selects the PI] → chargeId → sourceTransaction, fulfill pattern; Rule D allowlist entry removed) |
| VOR-19 | money-path | buyer/orders/[id]/cancel (orders flip :187) + vendor reject (orders flip :194) (found by money-structure Rule B, 2026-07-13) | CHK-1 family, 4th+5th sites: buyer-cancel and reject can cancel a PENDING (unpaid) order without expiring its live Stripe session → stale tab pays a cancelled order (relies on the still-deferred CHK-1 webhook paid-flip guard to avoid re-activation). | sessions.expire before the order flip when status was pending (CHK-18 pattern); or land the CHK-1 3-way webhook guard which defuses all sites. | S each | **fixed** (2026-07-14: both sites expire the stored session before the all-items-cancelled order flip, scoped to status='pending' + session id present; expire-throw → ERR_CHECKOUT_005 logError + skip the flip [order may be race-paid, webhook finalizes]. Both files moved to Rule B ENFORCED, KNOWN_GAPS now empty. CHK-1 3-way webhook guard still the remaining defense-in-depth) |
| VOR-20 | money-path (note) | lib/payments/tip-math.ts:22 (documented during conservation tests 2026-07-13) | Per-item tip share = round(tip/N) does NOT conserve exactly: Σ shares can differ from the tip by up to ⌈N/2⌉ cents (over- or under-pays vendors by ≤6¢ on a 12-item order; platform absorbs). Bounded + asserted in pricing-conservation.test.ts. | If it ever matters: zero-sum split like proratedFlatFee. | S | wontfix-for-now (bounded, documented, tripwired) |
| VOR-15 | money-path | fulfill/route.ts non-23505 insert-failure branch (was :310; found during VOR-3 fix 2026-07-12) | Same class as VOR-3b but in FULFILL: on a non-23505 vendor_payouts insert error it crumbs and CONTINUES — transfer fires with payoutRecord=null → money moves with no tracking record (invisible to retry cron/reconciliation). buyer-confirm now throws here; fulfill should match. | Make non-23505 insert error fatal via traced.fromSupabase BEFORE transfer (mirror buyer-confirm VOR-3 fix). | S | **fixed** (throws traced.fromSupabase before transfer; trade-off accepted: item already flipped fulfilled → retry gets 'Already fulfilled', payout needs admin — but loud [error_logs] and no untracked money, consistent with buyer-confirm) |

---

## Slice 1 — Checkout & payments (Fable, 2026-07-12)

Coverage note moved above; row CHK-17 continues below.

| ID | Cat | Conf | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|---|
| CHK-17 | money-path | Confirmed(F) | stripe/market-box-payout.ts:134-140 | Failed MB transfer logged with `console.error` only (not `logError`) → invisible to error-log review; webhook already 2xx'd (no Stripe retry) → vendor unpaid unnoticed. | Swapped console.error → logError(TracedError) — market-box-payout.ts:135. (cron-retry for failed MB payouts still a follow-up.) | S | **fixed** |

### Post-push review of the slice-1 batch (Fable, 2026-07-12) — commits 7ec3243f..42cdbf51 + b16c8ebc re-reviewed
All verified sound (CHK-2 vars declared before use; CHK-3 safe — cart_items SELECT RLS scopes via carts.user_id, mig 20260129_004:45-52; CHK-4/5/16 correct; VOR-3 swaps clean). Items raised:

| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| CHK-18 | observability | checkout/session cleanup loop (from CHK-1 fix ccf1bdee) | Bare `catch { return }` on sessions.expire swallowed ALL errors unlogged — completed-session race (expected) indistinguishable from Stripe API failure; skipped cleanup left pending order + held inventory with no diagnostic trail. (Null-session-id case does NOT exist — query filters `.not('stripe_checkout_session_id','is',null)`, session:123.) | logError(ERR_CHECKOUT_005 w/ order+session id + Stripe message) before the safe skip; ERR_CHECKOUT_005 cataloged. | S | **fixed** |
| CHK-19 | ops-risk (resolved by policy) | webhooks.ts CHK-9 guard (1ba1e50f) | Renewal refuse-on-missing subscription metadata.vertical would silently downgrade any ACTIVE vendor sub created before the session-75 vertical guard (f58e54cf). | USER DECISION 2026-07-12: all legacy paying vendors are testers, wiped before relaunch → risk moot. No code change. If any pre-relaunch sub survives the wipe, backfill `vertical` in its Stripe subscription metadata. | — | **wontfix** (policy) |
| CHK-20 | observability | market-box-payout.ts:59,75,103,188 | ERR_PAYOUT_003/004/006/007 also uncataloged (005 now cataloged); logError works, but error-log review lacks guidance entries for them. | Add the 4 sibling catalog entries. | S | **fixed** (all 4 added to market-box-errors.ts with abort-point + recovery guidance) |

---

## Slice 8 — Crons (Fable finder, 2026-07-12)

Coverage: vercel.json (5 registrations = 5 route files, none unregistered); expire-orders (all 21 phases), surveys, park-docs-review, vendor-activity-scan, vendor-quality-checks; libs cron/{order-timing,no-show,retention,external-payment,quality-checks-logic}, markets/{park-standing,park-checkin-reminders,park-docs-review}, surveys/cron-helpers, lib/inventory; cross-reads of fulfill/confirm/ready/buyer-confirm for the paid-gate contrast. **Clean:** CRON_SECRET timing-safe on all 5; Phase 3.6 claim-first idempotency; Phase 18 reconcile (budget-capped, status-guarded, ERR_RECONCILE_001); park-standing status-guarded release/suspend; no-show tz local-vs-local correct; Phases 4/4.6/15.5/20 per-market tz cutoffs hold (migs 184/185 intact); retention bounded. **Carryover answers: A = CHK-1 cron gap CONFIRMED (→CRN-2); B = Phase 5 payout-retry cron EXISTS and works (:1122-1544 — failed + pending_stripe_setup + MB mirrors + stale-row rescue + >7d admin alert); C = tz clean, but Phase 2/4/7 destructive updates lack status guards (→CRN-2/CRN-9).** CRN-1 and CRN-9 anchors re-verified against live code by the main session same day.

### P0
| ID | Cat | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| CRN-1 | money-path | expire-orders:764-806 (gate :764, transfer :790-795); :1710-1753 (gate :1710, transfer :1733-1738) | **VOR-1 class in the cron:** Phase 4 (no-show) + Phase 7 (auto-fulfill) payout transfers gate only on `stripe_checkout_session_id` (set at creation, exists on UNPAID orders) and transfer WITHOUT source_transaction = platform balance. Reachable: vendor confirm/ready check item status only; buyer-confirm normal ack path has no paid check (VOR-1 gate covers only the edge branch). Unpaid order → confirm → ready → pickup passes → cron pays vendor from platform money. | Port the ERR_ORDER_007 paid-gate (fulfill pattern) before BOTH cron transfers; add chargeId sourceTransaction (Phase 5's own :1181-1204 pattern). | M | **fixed** (gate before flip in Phases 4 AND 7, scoped to session-id orders so external/company-paid unaffected; blocked payout logs ERR_ORDER_007 + skips; both transfers now pass chargeId sourceTransaction) |

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| CRN-2 | money-path | expire-orders:318-364 (restore :335, items :338-347, order :350-353) | **CHK-1 residual (carryover A):** Phase 2 cancels stale pending Stripe orders with NO sessions.expire() and NO status guard on the cancel updates → stale tab pays a dead order (+ mid-run payment overwritten paid→cancelled). | Mirror checkout/session:110-127 (expire first, CHK-18 logging); `.eq('status','pending')` on both updates. | S | **fixed** (sessions.expire before cancel, throw→ERR_CHECKOUT_005 logError + skip order; order-cancel guarded `.eq('status','pending')`; item-status filter deliberately NOT added — confirmed/ready items on a dead order must still cancel) |
| CRN-3 | correctness | expire-orders:99-128 | Quick-check work gate counts only 6 work types; 15+ phases' work never counted (pending booth_booking_groups/Phase 18, tier expiry/8, catering 11-15.5, booth rentals/16, invitations/17, credits/19, season-end/20, retention/9) → on a quiet platform the whole run no-ops daily; a PAID-via-missed-webhook season stays pending forever. | Count the missing work types, or gate only Phases 1-7. | S | **fixed** (2026-07-18: gate REMOVED entirely — every phase self-gates on its own cheap query. Coded business rule IR-R20 asserted the old gate → genuine conflict, presented; USER-APPROVED rule update, test rewritten to assert NO global gate (decisions.md 2026-07-18). Process note: deviation from the approved "gate 1-7" shape was built before asking — user feedback logged, memory `feedback_ask_before_known_deviation`) |
| CRN-4 | correctness | expire-orders:2186-2316 (skip :2211-2213, flip only at :2305-2310) | Phase 12 organizer "event results" email re-sends DAILY forever when no vendor accepted (no sent-marker; status flips to ready only on acceptances). | Stamp results_sent_at / terminal status after any send. | S | open |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| CRN-5 | data-integrity | expire-orders:335→338-353, :417→419-435; inventory.ts:57-70 | Phases 2/3 restore inventory BEFORE cancelling (CHK-7 sibling); crash/timeout between (see CRN-10) → next run double-restores → phantom inventory/oversell. | Cancel-first guarded claim; restore only guard-matched rows. | M | **fixed** (2026-07-18: both phases → `cancelOrderItemsAndRestoreGuarded` (CHK-7's shared helper); Phase 3's order flip also gained the missing `.eq('status','pending')` guard → Rule A allowlist count 2→1 per rot-check) |
| CRN-6 | correctness | expire-orders:158-161 vs :295-301 | Phase 1 vendor join selects only `id, profile_data` → `vendor?.user_id` always undefined → vendor "order expired" notification has NEVER sent. | Add user_id to the select. | S | **fixed** |
| CRN-7 | observability | expire-orders:1218-1226, :1365-1372 (bare catch, ZERO log); :801-805, :1746-1752 (console.error); all phaseNError catches | Money-phase failures invisible to error_logs: Phase 5 retry transfer failures swallowed with no log at all; Phase 4/7 transfer failures + 21 phase-catches console.error only. | logError(TracedError) at money catch sites (ERR_REFUND_001 :267-271 is the in-file model). | S | **fixed** (5 sites: Phase 5 failed-retry + pending_stripe_setup + MB-retry catches, Phase 4 + Phase 7 transfer catches; new ERR_PAYOUT_008 cataloged, MB retry uses ERR_PAYOUT_005) |
| CRN-8 | money-path | expire-orders:1269-1274 vs :1181-1204 | Phase 5 pending_stripe_setup retry is the ONLY of 4 retry branches transferring WITHOUT chargeId source_transaction → platform-balance draw / balance_insufficient churn (Session-74 pattern). | Copy the chargeId lookup from the failed-branch. | S | **fixed** |
| CRN-9 | correctness | expire-orders:751-754, :994-997, :1681-1687 | **VOR-2 class in the cron:** Phase 4/4.6/7 item status flips unconditional (no status/cancelled_at guard/rowcount) → refund landing mid-batch overwritten to fulfilled → vendor paid for refunded item. Verified by main session. | Guarded updates + rowcount; skip payout on 0 rows. | S | **fixed** (Phase 4 eq 'ready', Phase 4.6 eq 'confirmed', Phase 7 in ready/confirmed; all + cancelled_at null + rowcount→skip payout/notify) |
| CRN-10 | correctness | expire-orders:59 vs :1130-1133, :2740 | maxDuration=60 < file's own worst-case phase budgets (Phase 5 ~30s + Phase 18 ~25-50s alone) → mid-run kill: tail phases (19/20/21 — this cron is their only driver) silently never run; mid-Phase-2 kill triggers CRN-5. | Raise maxDuration (Pro allows 300) + elapsed-time check between phases. | S | **fixed** (2026-07-18: maxDuration 60→300; 270s soft budget checked before Phases 4/5/7/10/16/18 → logError ERR_CRON_001 (cataloged) naming the last completed phase + partial summary return, instead of a mid-phase hard kill) |
| CRN-11 | efficiency | surveys:429-436,461-465,543-550,568-572 | Known surveys N+1 (per-recipient count query + email lookup + individual sends) — filed to reach the ledger. | Batch prefetch + sendNotificationBatch. | M | open |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| CRN-12 | efficiency | surveys:213-225 | Market-day follower reminders loop sendNotification per follower (unbounded, hourly). | sendNotificationBatch. | S | **fixed** (2026-07-17: switched to sendNotificationBatch — uniform payload per follower; N×~2 profile reads → 2. Now genuinely cheap thanks to NOT-2) |
| CRN-13 | correctness | surveys:52-67, park-docs-review:17-31 vs expire-orders:64-66 | 2 of 5 crons lack the VERCEL_ENV production gate the other 3 have; manual/staging invoke of surveys emails real addresses in staging data. | Add the same gate. | S | open |
| CRN-14 | correctness | vendor-quality-checks:93-96 vs :141-149 | Findings superseded BEFORE new checks insert; check/insert failure leaves vendor findings empty + notifies about findings that failed to insert. | Supersede after successful insert; skip notify on insert failure. | S | **fixed** (2026-07-18: supersede moved AFTER successful insert, `.neq('batch_id', batchId)`; insert failure now THROWS → existing catch marks scan failed, no notify, old findings intact. Worst case on supersede failure = brief old+new duplicates, never an empty list) |
| CRN-15 | correctness (Medium conf) | expire-orders:1819-1836 | Phase 8 tier expiry fetch-then-unconditional-update; renewal webhook extending tier_expires_at mid-window clobbered. | `.lt('tier_expires_at', now)` guard on the UPDATE. | S | open |
| CRN-16 | efficiency | expire-orders:2828-2830, :2645 | Phase 19 fetches ENTIRE booth_credits ledger daily, sums in JS (unbounded growth); Phase 16 per-row auth.admin.getUserById. | SQL aggregate; batch auth lookups. | S/M | **fixed** (2026-07-18, mig 198 `get_booth_credit_expiry_state` — exact SQL mirror of the JS semantics, positive balances only; RPC missing → logError ERR_CRON_002 + skip phase (pre-mig-safe). Warnings loop N+1 → 2 batched .in() queries. Phase 16: one batched user_profiles email lookup (EVT-16 pattern) replaces per-row auth.admin.getUserById) |

Cross-refs: CRN-2↔CHK-1 (both halves needed); CRN-5↔CHK-7; CRN-1/9↔VOR-1/2; VOR-15 cron-side checked = safe (null payoutRecord skips transfer).

---

## Slice 6 — Market-box / subscriptions (Fable finder, 2026-07-12)

Coverage: market-box-payout.ts, webhook-utils.ts, MB+subscription branches of webhooks.ts, MB portions of checkout/success+session, payments.ts MB helpers, pricing MB fns; all MB routes (buyer/market-boxes/{route,[id],confirm-pickup}, market-boxes/*, vendor/market-boxes/{route,[id],pickups,pickups/[id],skip}); subscriptions/{checkout,verify}, vendor+buyer subscription/status + tier/downgrade; migs 006/018/059/124/125/127/149/152/163 + 063; MB-table RLS; MarketBoxDetailClient purchase path. **Clean:** RLS/service-client discipline sound across the slice (no VOR-3 class); offering CRUD guards (tier caps, active-sub guards); subscribe RPC FOR-UPDATE + idempotent; financial RPCs anon-revoked; CHK-8/9 fixes present; subscription.deleted refuses on missing vertical. **Question answers: A = both checkout paths WRITE basePriceCents but the standalone webhook never READS it (→MBX-1); the M9 warning fn is dead code (→MBX-6). B = mig 124 biweekly end-date formula SUPERSEDED by migs 125/163 (`term_weeks*7`); original_end_date is display-only (completion=pickup counts, auto-miss=scheduled_date+grace, capacity=active-count); remaining code bug is MBX-2; the backlog product decision is essentially RESOLVED by mig 125's term-duration semantic. C = double-transfer + refund-vs-payout VERIFIED CLEAN (existence check + partial unique idx mig 059 + deterministic transfer key `transfer-mb-sub-${subId}` + per-item mutual exclusion).** MBX-1 anchors re-verified by main session same day.

### P2
| ID | Cat | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| MBX-1 | money-path | webhooks.ts:389,415,491 vs payments.ts:193-195, buyer/market-boxes:313-320 | **Standalone MB checkout overpays vendor / under-collects platform fee:** handleMarketBoxCheckoutComplete uses metadata.price_cents (buyer FEE-INCLUSIVE total) as actualPaidCents, never reads base_price_cents. $10 base → vendor gets payout on 1080¢ (995¢) not 1000¢ (920¢); platform nets 85¢ not 160¢. Also seeds p_total_paid_cents fee-inclusive (:442) → buyer GET fallback double-applies fees. Reachability: standalone POST route is live+authed but UI-orphaned (detail page uses cart path — verified by grep). | Wire base_price_cents read into the handler (optionally via the currently-dead selectBasePriceForTermWeeks = MBX-6), observable fallback (logError) if absent; pass base to p_total_paid_cents. Or 410-stub the orphaned route. | M | **fixed** (base resolved via selectBasePriceForTermWeeks [MBX-6 wired = no longer dead] before both payout branches + p_total_paid_cents; missing-meta fallback logErrors ERR_WEBHOOK_004; refunds intentionally stay fee-inclusive priceCents; frequency lookup merged into the same offering query) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| MBX-2 | correctness (display) | buyer/market-boxes/[id]/route.ts:120-126 | Single-sub GET recomputes end_date = start+(totalWeeks-1)*7, ignoring original_end_date → disagrees with DB (term_weeks*7) AND the list endpoint; biweekly 4-week: shows day 21, DB 28, last pickup 14. | Return original_end_date; drop weekly assumption. | S | **fixed** (select += original_end_date, used directly; weekly recompute kept only as legacy-null fallback) |
| MBX-3 | data-integrity | webhooks.ts:856-862,920-923 | handleTransferCreated/Failed update ALL vendor_payouts rows for a market_box_subscription_id (not scoped to the transfer) → historical failed rows flipped completed / completed flipped failed → reconciliation noise. | Scope update by stripe_transfer_id. | S | **fixed** (created: status-scoped `.in('pending','processing')` — transfer-id scoping would race our own post-transfer id write; reversed: `.eq('stripe_transfer_id', transfer.id)` — precise, id long-persisted by reversal time) |
| MBX-4 | observability | pickups/[id]/skip:108; pickups/route.ts:152 | vendor_skip_week RPC failure (mutates pickups + extensions) + pickups-list fetch error are console.error only. | logError both sites. | S | **fixed** (logError(traced.fromSupabase) both sites) |
| MBX-5 | security/correctness | subscriptions/verify/route.ts:71-85 | Fallback tier activation lacks the CHK-9 refuse-on-missing-vertical guard — session w/o vertical updates BOTH multi-vertical profiles by user_id. New sessions safe (checkout:61 enforces); legacy-session risk only (CHK-19 policy applies). | Mirror webhook refuse+logError. | S | **fixed** (refuse + ERR_WEBHOOK_002 logError + 400; vertical now mandatory `.eq` on the update) |
| MBX-6 | dead-code | webhook-utils.ts:45-66 | selectBasePriceForTermWeeks + M9 warning have NO production caller (test-only import); base_price_cents written by both paths, read by none. | Wire into MBX-1 fix (makes it load-bearing) or delete fn+metadata key. | S | **fixed** (wired into handleMarketBoxCheckoutComplete via MBX-1 — now load-bearing; fn itself untouched [pure, tested]) |

---

## Slice 4 — FT park-operator (Fable finder, 2026-07-13)

Coverage: all manager park routes (park-spots, [spotId], park-mode, vetting, bar, standing-reservations PATCH, attendance), vendor routes (book-park-spot, standing-reservation, pay-occurrence, checkins), money path (createParkSpotCheckoutSession payments.ts:458-544, webhooks park_spot branch :1490-1637, pricing.ts:275-357 + tests), libs (park-standing, park-week-schedule, park-checkin-reminders, park-docs-review, checkin-eligibility, park-booking-types), migs 160/171-174/177/179/180/182, admin keep-pct lever, BookParkSpotForm + ParkWeekCard contracts. **Question answers: A = operator_keep_pct money math VERIFIED CLEAN end-to-end (admin+DB CHECK 0.935-1.0; float-safe pct pricing.ts:344-347; transfer < charge always incl. keep=1.0; $5 min charge blocks Stripe floor; platform nets charge−transfer, no webhook math). B = no VOR-1 analog (only status='paid' writer is the guarded webhook flip :1547-1555); flips guarded EXCEPT standing PATCH (→PRK-8); pay idempotency intact. C = RLS/client discipline VERIFIED CLEAN (4 park tables default-deny + service-client everywhere behind auth; checkins vendor-own policies). D = 4 lifecycle edges (PRK-1/4/5/7).** Clean also: authz/cross-park spoofing guards everywhere, atomic booking RPC, deterministic keys, component contracts. Noted not filed: park_spot_bookings 'completed' never written (dormant value). PRK-1 + PRK-3 anchors re-verified by main session same day.

### P1
| ID | Cat | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| PRK-1 | money-path | park-standing.ts:236-243; pay:125-129; webhooks:1536-1543 | **CHK-1/CRN-2 class, 3rd site:** prepay-cutoff sweep flips occurrence→expired WITHOUT expiring the stored Stripe session → truck pays a stale session for a RELEASED slot → ERR_WEBHOOK_014 manual reconciliation, transfer to operator already executed, slot possibly resold (double revenue), truck keeps the strike. | sessions.expire() before the flip (CHK-18 logging pattern); consider auto-refund on the 014 path. | M | **fixed** (sweep expires the stored session before EVERY release — standing + one-off; expire-throw → logError ERR_CHECKOUT_005 + skip release [possibly race-paid]; auto-refund on 014 NOT added — 014 now near-unreachable + cataloged with refund-vs-honor guidance) |
| PRK-2 | data-integrity | mig 172:69-74; park-standing.ts:232; book-park-spot:308-319 | **Abandoned one-off checkout locks the slot FOREVER** — no code path expires/cancels a one-off pending_payment booking (sweep is standing-only; bar requires paid; no cancel API). Phantom 'unpaid' truck on week card; availability DoS by start-and-abandon. | TTL-sweep one-off pending rows → expired (WITH PRK-1 session-expire); and/or cancel endpoint. | M | **fixed** (sweep release section now covers one-offs: created_at > 24h old [Stripe session max age] OR booking_date past → session-expire + guarded flip to expired; one-off expired rows never strike [strike query filters by standing_reservation_id]. Cancel endpoint NOT built — sweep suffices) |
| PRK-3 | correctness/contract-break | checkin-eligibility.ts:95-112; book-park-spot:173-178; checkins:92-97; park-standing.ts:95-102,185-192 | **Paid park trucks can't check in** (eligibility = approved market_vendors OR FM weekly rentals; book-then-vet affiliates approved:false; park_spot_bookings never consulted) → attended days = false no-show strikes → auto-suspension of paying anchors in ~3 weeks; reminder cron nudges toward a 403. | Add paid park_spot_bookings (market-local today) as an eligibility source. | S-M | **fixed** (source (c) in getEligibleCheckInMarkets: paid bookings for todayStr, spot label doubles as booth display; same default-tz day convention as source (b)) |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| PRK-4 | contract-break/security | park-standing.ts:247-303; pay:40-94; park-vetting:48-74 | Vetting BLOCK doesn't reach standing holds — blocked truck's hold keeps generating occurrences + truck keeps paying them. | Sweep skips blocked (market,vendor); pay re-checks; block optionally auto-suspends holds. | S | **fixed** (sweep batch-fetches blocked vetting pairs + skips generation; pay route re-checks blocked → 403; auto-suspend-on-block NOT added — generation stops, existing occurrences unpayable via pay-gate) |
| PRK-5 | money-path | pay:45-53 | Revoked/suspended hold's already-generated pending occurrence stays payable → truck pays + attends despite revocation; manager's only tool is no-refund bar. | On revoke/suspend expire future pending occurrences (with session-expire); pay re-verifies hold active. | S-M | **fixed via the pay-gate** (pay re-verifies hold status=active → 409; money edge fully closed. PATCH-side occurrence expiry NOT added — the slot self-frees at the sweep cutoff ≤ ~2 days, acceptable) |
| PRK-6 | money-path | pay:125-132 vs webhooks:1514-1525 | Group-linkage update failure is logError-and-continue but the checkout URL is still returned → paid session unmatched (ERR_WEBHOOK_012), booking expires + strikes, manual reconciliation. | Make linkage failure fatal 500 pre-URL (retry safe — deterministic key). | S | **fixed** |
| PRK-7 | correctness | park-standing.ts:247-266 vs pay:62-67 | Park flipped to 'free' / Stripe-disabled: occurrences keep generating, pay refuses, every cutoff = strike → all anchors auto-suspended blamelessly. | Generation filter park_mode='paid' AND stripe_charges_enabled; don't strike unpayable occurrences. | S | **fixed** (generation skips non-payable parks — no occurrence → no expiry → no strike; pre-existing stranded pending rows expire once, accepted) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| PRK-8 | correctness | standing-reservations:119-134 | Manager approve/revoke/reinstate has NO status precondition — approve on suspended (stale UI) flips active w/o strike reset → instant re-suspend. | Per-action .eq('status', expected) + rowcount→409. | S | **fixed** (approve: requested only; reinstate: suspended only; revoke: requested/active/suspended; maybeSingle + 409 with current state) |
| PRK-9 | correctness | park-standing.ts:129-133,185-192; mig 179:47-48 | Manager-barred paid occurrence still counts as no-show strike when the truck obeys the bar — bar + strike + suspension stack on one incident. | Exclude manager_barred_at IS NOT NULL from no-show candidates. | S | **fixed** (.is(manager_barred_at, null) on the source-2 paid-rows query) |
| PRK-10 | data-accuracy | manager-dashboard-stats.ts:542,561 | Park earnings dashboard recomputes historical nets with CURRENT keep pct → past earnings display wrong after admin changes the lever. Display-only. | Snapshot effective keep/net at pay time, or label approximation. | S-M | open |
| PRK-11 | observability | webhooks:1635; book-park-spot:273,309; pay:134 | Park money-adjacent console.error sites (paid-notification block, RPC-empty 500, both session-create failures) invisible to error_logs. | logError swaps. | S | **fixed** (4 sites: webhooks paid-notification catch → ERR_WEBHOOK_015; book-park-spot RPC-empty → ERR_CHECKOUT_010 + session-fail → ERR_CHECKOUT_002; pay session-fail → ERR_CHECKOUT_002) |
| PRK-12 | observability | webhooks:1507,1521,1538,1559 | ERR_WEBHOOK_011-014 uncataloged (CHK-20 class) — 014 is the "charged, no pending rows, manual reconciliation" signal. | Catalog entries. | S | **fixed** (new catalog/webhook-errors.ts: ERR_WEBHOOK_011-015 with reconciliation guidance, registered in error-catalog.ts) |
| PRK-13 | efficiency | park-standing.ts:234-243,269-286; park-checkin-reminders:162-176 | Sweep N+1 (~4-5 queries/hold/day + per-row expiry UPDATE); reminder per-recipient sends (CRN-12 class). | Batch expiry UPDATE + prefetches; sendNotificationBatch. | S | open |

Cross-refs: PRK-1↔CHK-1/CRN-2; PRK-11↔CHK-17; PRK-12↔CHK-20; PRK-13↔CRN-12; park-docs prod gate = CRN-13.
| MBX-7 | data-integrity | webhooks.ts order-item branch (handleTransferCreated ~:886-892, found during MBX-3 fix) | Same unscoped-update class as MBX-3 but on the ORDER-ITEM branch: transfer.created flips ALL vendor_payouts rows for an order_item_id to completed, incl. historical failed retry rows. Outside the approved MBX batch — not fixed. | Same status-scoping as the MB branch. | S | **fixed** (created: `.in('status',['pending','processing'])`; ALSO the order-item reversal branch [handleTransferFailed] had the same bug — scoped `.eq('stripe_transfer_id', transfer.id)`, mirroring MBX-3's split rationale) |

---

## Slice 5 — Events (Fable finder, 2026-07-14)

Coverage: all 15 events/[token] routes (incl. NEW agreement/broadcast/ratings), buyer/events rate+review-state, lib/events/*, vendor/events/[marketId]/*, event-requests, event-approved-vendors, admin/events/[id]/{route,settlement,payments}, cron Phases 11-15.5 + 4/7 gates, company-paid branches of fulfill/buyer-cancel, ShopClient/EventRequestForm contracts, migs 070/091/094/110-121/130/149/152/189. **Question answers: A = NO other Stripe transfer/refund site reachable by company-paid orders (fulfill exempts :92,101,199-202; cron Phases 4/7 gate AND transfer scoped to session-id orders :800,847/:1817,1879 — company-paid orders never have one); VOR-14 remains the only transfer hole; the eventual VOR-10 fix must exempt company_paid; RPC fee math matches pricing.ts but hardcoded in SQL; organizer settlement never moves money in code (manual admin ledger, admin-gated) but under-invoices (EVT-11). B = new tokens ~108-bit; no expiry/revocation exists; money/PII capability needs auth+access-code or organizer identity ON TOP of the token; token-only surfaces = select POST (disruption, not money) + verify-code (5/min). C = reserve/cancel wave RPCs row-locked + capacity-CHECK-backstopped (clean); create_company_paid_order is the unlocked exception (EVT-17); free_wave coverage missing at buyer-cancel + resolve-issue (EVT-15); reservation TTL is dead code (EVT-8); capacity maintenance is dead code (EVT-9). D = agreement picker leak-proof both directions + Option-C snapshot ordering correct (respond:231-256); broadcast audited/capped/moderated/scoped (N+1 only, EVT-16); ratings read path approved-only+anonymous, but SUBMISSION dead for active/review events (EVT-6). E = cron status flips guarded; admin events PATCH allows any→any incl. completed re-fire (EVT-14); the 'cancelled' CHECK gap kills both cancel flows (EVT-3). F = session-expire missing at both event-cancel order-flip sites (part of EVT-4, VOR-19 family sites 6+7); console.error-only at order + waves/reserve routes (fold into EVT-1 batch); NO bare transfers in slice.** HEADLINE: company-paid ordering AND event cancellation have never been executable end-to-end (EVT-1/2 and EVT-3 respectively) — these are dead-feature breaks, not live money leaks. Anchors EVT-1/2/3/4/6/10/11/12 re-verified by main session same day.

### P0
| ID | Cat | Anchor | Claim / scenario | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| EVT-1 | money-path/contract-break | mig 119:147-162 (INSERT), :102-110 (cap check) vs snapshot orders :817-841 | `create_company_paid_order` INSERTs non-existent orders columns (user_id, market_id, buyer_fee_cents, service_fee_cents, vendor_payout_cents) + omits NOT NULL buyer_user_id; cap check queries o.user_id/o.market_id. plpgsql bodies not validated at CREATE → applied cleanly on all 3 envs; every attendee order 500s (route logs console.error only). **Company-paid ordering has never worked.** Columns verified absent vs snapshot (itself live-confirmed 2026-06-24 changelog). | Rewrite RPC against real schema (buyer_user_id; per-item money already goes to order_items; drop phantom cols) + EVT-17 FOR-UPDATE + route logError swap. information_schema check before composing. MIGRATION (user applies). | M | **deferred** (company-paid backlog package, user decision 2026-07-14 — see backlog.md) |

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| EVT-2 | contract-break | ShopClient.tsx:416-424 vs events/[token]/order/route.ts:64-73 | Order POST never sends access_code; route hard-403s when event.access_code is set (every company-paid/hybrid event gets one at approval) → confirm-order always fails even after verify-code succeeded. | Include the verified accessCodeInput in the POST body. | S | **deferred** (company-paid backlog package 2026-07-14) |
| EVT-3 | correctness/data-integrity | mig 094:74-76 (CHECK recreated WITHOUT 'cancelled'; grep confirms it's the only ADD of this constraint) vs events/[token]/cancel:63-72 + admin/events/[id]:277-297 + mig 121 trigger 'cancelled' branch | catering_requests status CHECK lacks 'cancelled' → BOTH cancel flows (organizer route + admin route) 500 on the status update before ANY cleanup runs. Supersedes/absorbs the backlog "'cancelled' admin status side bug" — it's the whole cancel feature, not a dropdown nit. | Migration re-adding 'cancelled' to the CHECK. MUST land WITH EVT-4. | S | **fixed** (mig 190 `efa6a7c6`, applied Dev+Staging 2026-07-14, Prod pending; shipped WITH EVT-4 by design — cancel flows live on staging) |
| EVT-4 | money-path | events/[token]/cancel:139-165 + admin/events/[id]:277-297 | Event cancel flips paid Stripe orders to cancelled with NO refund + no sessions.expire + order_items left live; the buyer notification text PROMISES "a refund will be processed"; un-cancelled items can later pass the cron Phase 4 paid-gate → vendor paid for a no-show at a cancelled event. Latent behind EVT-3 — goes LIVE the moment EVT-3 ships alone. | On cancel: refund succeeded payments (VOR-5B pattern), expire pending sessions (VOR-19 pattern), guarded-cancel the order_items. Both routes. | M | **fixed** (`efa6a7c6`, both routes: per-order pending→sessions.expire w/ skip-if-throw; Stripe-paid→REMAINING-balance refund [`${orderId}-event-cancel` key; fulfilled-item orders skip + logError manual review]; guarded item cancel; admin route also gained free_wave parity; both files → Rule B ENFORCED) |
| EVT-5 | money-path | vendor/events/[marketId]/cancel/route.ts:109-140 (no order_items/refund handling anywhere in file) | Vendor cancels an accepted commitment → their existing pre-orders untouched (buyers not refunded/notified, items stay confirmed/ready) → withdrawn vendor can be cron-auto-paid for items never fulfilled; wave slots never freed. | Cancel+refund the vendor's non-terminal items at commitment-cancel + free_wave; or mandatory admin follow-up flag. | M | **fixed** (`9c2f6c4c`: guarded per-item cancel + reject-math refunds [subtotal+%fee+prorated flat] + inventory restore + buyer notifications; fully-dead orders closed w/ VOR-19 session-expire + free_wave; wave capacity recalc'd [EVT-9]. Company-paid orders skip refund naturally — no succeeded payment row) |
| EVT-6 | contract-break/RLS (VOR-3 class) | buyer/events/[token]/rate:60-76 + review-state:39-47 (user client on catering_requests) vs mig 091:12-16 (SELECT policy = approved/completed only) vs ratable statuses active/review/completed (rate:70) | For 'active'/'review' — 2 of the 3 rating windows — the user-client lookup returns 0 rows → 404 → attendees cannot rate until 'completed' (3+ days post-event, Phase 15.5). mig 116 INSERT policy inherits the same blind spot. | Widen public_event_token_read to ready/active/review (migration) OR serviceClient + explicit status gate in both routes (code-only). | S | **fixed** (`cc0fdc5f`, code-only option: serviceClient lookups in rate + review-state; rate's write also serviceClient with route-enforced invariants — status gate, own-user_id, explicit pending-only edit lock [409] mirroring the mig-116 UPDATE policy; upsert error now traced not console) |
| EVT-12 | contract-break (EVT-1 root class) | vendor/events/[marketId]/prep/route.ts:105-107 | Prep sheet embeds `orders!inner(... user_id ...)` — non-existent column → PostgREST 42703, error discarded → prep sheet shows zero orders for every event vendor. | Drop user_id from the embedded select (unused in the file). | S | **fixed** (`cc0fdc5f`; grep-verified unused) |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| EVT-7 | correctness | wave generation only at admin/events/[id]:196-213 (+manual route); self-service ready-flips generate nothing (respond:401-408, select:248-271, cron:2479-2484); EventRequestForm:207 hardcodes self_service; ShopClient:322 gates company-paid on waves | Self-service company-paid events never get waves → ShopClient silently degrades them to attendee-paid (attendees personally pay at a company-paid event) unless an admin manually intervenes. | generateEventWaves on all ready-flips when payment_model=company_paid (shared helper), or block the self-service+company-paid combination. | M | **deferred** (company-paid backlog package 2026-07-14) |
| EVT-8 | data-integrity | mig 120:9-16,62-63 (expires_at written, cron cleanup promised in comment) — zero readers in src (grep) | Wave-reservation expiry never enforced → abandoned 'reserved' rows hold reserved_count forever + UNIQUE(market,user) permanently blocks that user from re-reserving. Availability-DoS analog of PRK-2. | Cron sweep: cancel reserved rows past expires_at via a guarded RPC that reopens capacity; or lazy-free inside reserve_event_wave. | S-M | **fixed** (`9c2f6c4c`, code-only both halves via the EXISTING row-locked cancel_wave_reservation RPC: waves/reserve lazy sweep at point of need [frees the caller's own stale row] + cron Phase 13.5 daily global backstop) |
| EVT-9 | correctness | recalculate_wave_capacity has no caller (grep); vendor cancel adjusts nothing (:109-116); generation counts backups (wave-generation:91-95,132; select:237-244 leaves non-selected 'accepted'); mig 130:49-51 never reopens 'full' status | Wave capacity drifts 3 ways: inflated by backup vendors at generation, never reduced when a vendor cancels (overbooks the rest), and a 'full' wave never reopens after a capacity raise. | Fix recalc RPC (exclude is_backup, recompute status) + call it from vendor cancel/respond + admin cap changes. | M | **fixed** (`9c2f6c4c`: mig 191 [applied Dev+Staging 2026-07-14, Prod pending] — backup exclusion + mig-130 RAISE kept + open↔full status recompute; generateEventWaves same exclusion; RPC's first callers wired: vendor commitment-cancel, respond, organizer select — each guarded to wave-bearing events, RAISE logged non-fatal) |
| EVT-10 | correctness (CRN-6 class) | events/[token]/cancel:104-110 passes v.vendor_profile_id as sendNotification userId (service.ts:398-399) | Event-cancel vendor fan-out targets profile ids, not user ids → vendors never receive the cancellation notice (send swallows). Admin cancel path has NO vendor fan-out at all → no path informs vendors an event was cancelled. | Join vendor_profiles(user_id) (broadcast:125 pattern); add vendor fan-out to the admin cancel path. | S | **fixed** (`cc0fdc5f`: organizer route joins user_id + skips missing; admin cancel/decline path gained the vendor fan-out it never had) |
| EVT-11 | money-path (fee-inclusive class) | admin/events/[id]/settlement:345-347,445 vs mig 119:139-144 | Settlement report balances company payments against Σ subtotal_cents, omitting the buyer-side 6.5%+15¢/order that mig 119 defines the company as owing → platform systematically under-invoices company-paid events ~6.7%. | Balance against Σ(subtotal+buyer fee+flat) for company-paid items (or orders.total_cents once EVT-1 lands it correctly). | S | **deferred** (company-paid backlog package 2026-07-14) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| EVT-13 | security | event-actions.ts:70-77 (Math.random access code), :98-113 (legacy weak tokens remain valid) | Access code non-CSPRNG, 8-char/32-alphabet; verify-code is 5/min but the order route uses the general api limiter = a second guessing surface. Legacy weak tokens moot (tester-wipe policy). | crypto.randomBytes for codes; optionally the auth limiter on the order route. | S | **deferred** (company-paid backlog package 2026-07-14) |
| EVT-14 | correctness | admin/events/[id]:320-329 (completed effects lack a prior-status guard; contrast cron :2648-2654 + approve guard :114) | Admin re-PATCH containing status:'completed' re-fires runEventCompletionEffects → duplicate feedback/settlement notifications to every buyer + vendor. | Guard on prior status (mirror the approve guard). | S | **fixed** (`cc0fdc5f`: `cateringReq.status !== 'completed'` added to the completed-effects condition) |
| EVT-15 | money-path/data-integrity | free_wave_on_order_cancel callers = exactly 3 (grep): reject:242, events/cancel:161, cancel-date-cascade:185. Missing: buyer-cancel (which also has no company-paid branch — VOR-14 sibling) and resolve-issue | Buyer cancelling a company-paid order leaves the wave slot 'ordered' + UNIQUE blocks them from re-ordering at that event; same for issue-refund cancels; buyer-cancel also walks its Stripe-refund math on paymentless company-paid orders. | free_wave (service client) at both cancel branches + a company-paid early-branch in buyer-cancel mirroring fulfill's. | S each | SPLIT 2026-07-14: **wave-half fixed** (`9c2f6c4c`: free_wave_on_order_cancel at buyer-cancel + resolve-issue last-item branches — no-op for reservation-less orders); company-paid early-branch half deferred to the backlog package |
| EVT-16 | efficiency | broadcast:174-196 (per-recipient admin.getUserById + send; sendNotificationBatch exists service.ts:618); complete-event.ts:54-66,149-156,182-201; my-order:45-50 (scans ALL event items per view) | Event fan-outs are per-recipient (300-attendee broadcast ≈ 600 extra calls → ~3 batched); my-order full-scans the event's items per page view. | Batch prefetch + sendNotificationBatch; filtered join for my-order. | S-M | **fixed (broadcast half)** (2026-07-17: BOTH broadcasts [market-manager + events/[token]] switched to sendNotificationBatch — dropped the per-recipient `auth.admin.getUserById` [N auth API calls → 0; email now resolves from user_profiles, same source as every other notification] + per-recipient profile query. 300-attendee broadcast: ~600 calls → ~2+N sends. **ALSO fixed 2026-07-17:** complete-event event_feedback_request (buyers) + admin-unfulfilled → sendNotificationBatch (uniform); vendor-unfulfilled per-vendor user_id lookup N+1 → single `.in()` query; my-order route no longer scans ALL event items per page view — order_items scoped to the buyer via `orders!inner` filter. EVT-16 fully closed) |
| EVT-17 | correctness (race — fold into EVT-1) | mig 119:53-68 (no FOR UPDATE on reservation), :102-120 (cap TOCTOU), :180-182 (unguarded final UPDATE); contrast mig 120:32-36 | Two concurrent create_company_paid_order calls on one reservation both pass the 'reserved' check → duplicate orders, company pays twice. | FOR UPDATE + guarded UPDATE w/ rowcount inside the EVT-1 rewrite. | S (w/ EVT-1) | **deferred** (company-paid backlog package 2026-07-14) |

Minor unfiled notes: Phase-11 prep-reminder dedup is per-user-24h not per-event; my-order reads vestigial listings.image_urls (always null); FM catering-item thresholds inconsistent (respond 1+ vs autoMatch/event-approved-vendors 4+); mig-121 trigger over-counts freed reservations (GREATEST-floored, market dead anyway); event-approved-vendors omits deleted_at filter on vendor_profiles. Cross-refs: EVT-4↔CHK-1/VOR-19 (sites 6+7); EVT-6↔VOR-3; EVT-8↔PRK-2; EVT-10↔CRN-6; EVT-11↔CHK-6/MBX-1 class; EVT-15↔VOR-14; VOR-10's eventual fix must exempt payment_model=company_paid.

---

## Slice 3 — Market-manager / FM booth+season (Fable finder, 2026-07-15) — ✅ MAIN-SESSION ANCHOR-VERIFIED 2026-07-16, all 10 CONFIRMED at their anchors

**Verification notes (2026-07-16, beyond the finder's claims):** MGR-3 port must be the FULL VOR-16 cron-Phase-1 shape (tip + recomputed small fee) — cancel-date's per-item refund (cascade:101-103) includes the prorated FLAT fee only, NO small-order-fee share (contrast buyer-cancel's cancellation-fees.ts:72-73, which is why THAT site got tip-only). MGR-2 has a third poison path: book-season's Stripe-fail cleanup (`cancel_season_group`, book-season:272) leaves cancelled children that block those weeks; the one-off book route avoids this by DELETING its row on Stripe-fail (book:488-493). MGR-4 can pre-check cheaply — the handler's existing status read (webhooks:1253) already has 'cancelled' in hand before the flip; rowcount check still needed for the race window.

Coverage: all non-park market-manager/[marketId] routes (booth-inventory±[inventoryId], booth-labels/placeholders, branding/logo/documents/onboarding-acks/vendors/vendor-docs, broadcast, cancel-date, schedules, seasons+settlement+makeup-dates, stripe onboard/status, vendor-approval/booth/tier/invitations, weekly-rental) + public intake; vendor-side book/book-season/seasons/respond/join + booth-groups/[groupId]/cancel; libs manager-auth/manager-queries/season-*/settlement-math/cancelled-days/cancel-credit/cancel-date-cascade/booth-credit-balance + manager-dashboard-stats booth sections; pricing.ts:294-357, payments.ts:287-456 (booth/season checkout), webhooks.ts booth/season branches + account.updated; cron Phases 16/17/18; migs 137/139/142/165/166/167/168/169/186. **Question answers (finder): A = booth money math CONSERVED end-to-end (vendorPays=round(base×1.065)+15; managerReceives=base−fee; credit reduces both sides equally so the platform fee is invariant; credit capped min(managerReceives, vendorPays−50¢); no fee-inclusive confusion in money MOVEMENT — display only, MGR-8). Refunds: deliberately credit-only on the manager-held base (locked 2026-06-27), no Stripe-backward path. B = lifecycle covered by Phase 16 (guarded) + Phase 18 (FOR-UPDATE reconcile) EXCEPT MGR-2 vendor lockout + MGR-4 missing cancelled-conflict tripwire; no double-sell possible (advisory-locked RPC counts pending+paid). C = bookings gated on markets.stripe_charges_enabled but account.updated never syncs markets (MGR-5, PRK-7 analog — availability/observability, not money loss); intake→admin-approval→Stripe-unlock chain sound + fraud checklist. D = authz UNIFORM: all 34 gated routes call isMarketManager (manager_user_id OR verified-email match, AND manager_status='active'); child resources market-scoped; email backfill safe (strict-equality gate precedes the ilike oddity). E = credit redemption race-safe (advisory xact lock, migs 168/169); double-mint possible at group-cancel (MGR-1); silent credit destruction at 2 unchecked release inserts (MGR-7). F = unguarded flips only MGR-1 + MGR-6; session-expire gaps only MGR-3b + the Phase-16 timing note (in MGR-4); NO VOR-3 RLS-client class; NO wrong notification user_ids; NO enumerate-from-memory ops.**

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| MGR-1 | money-path/data-integrity (VOR-8 class) | vendor/booth-groups/[groupId]/cancel/route.ts:45 (plain status read), :112-121 (credit mint), :127-135 (D5 release mint), :141-150 (unguarded flips, no rowcount) | Vendor season-cancel is read-check-write — concurrent double-submit mints the cancel credit (and the D5 release) TWICE for one cancellation; balance redeemable against future bookings, funded from the manager's future transfers. | Claim first: flip group `.eq('status','paid')` + rowcount→409 BEFORE minting; guard the child flips the same way. | S | **fixed** (2026-07-16 batch 1: claim-first group flip + rowcount→409 before both mints; child flips guarded `.eq('status','paid')`. Trade-off accepted: failure between claim and mint = credit lost but loud [traced throw → error_logs], retry 409s — VOR-15 stuck-but-loud precedent) |
| MGR-2 | correctness/money-path | mig 139:83 (plain UNIQUE vendor+market+week counts cancelled rows); mig 186:222-245 (unique_violation→DUPLICATE, no revive); expire-orders:2740-2767 (Phase 16 keeps the cancelled row; the :2719-2720 comment claiming the UNIQUE frees up is FALSE); book:325-333 ("contact the market manager" but no manager tool exists); mig 165:97-107 (season path inherits → whole season purchase fails on the poisoned week) | A cancelled weekly rental PERMANENTLY locks that vendor out of that (market, week) — abandon checkout once, never book that week again; any season containing it also fails. Capacity unaffected (mig 186:110 counts pending/paid only) — the lockout is vendor-specific. | Migration: partial unique index `WHERE status IN ('pending_payment','paid')` (decide 'completed'), or a revive path in the RPC. | M | **fixed** (mig 193 applied Dev+Staging 2026-07-16, Prod pending): partial unique idx `uq_wbr_vendor_market_week_active` WHERE status IN (pending_payment/paid/completed — 'completed' included defensively), created BEFORE dropping the mig-139 plain UNIQUE (name discovered from pg_constraint by column set, no hard-coding). No companion code needed; RPC DUPLICATE mapping + capacity counting unaffected |
| MGR-3 | money-path (VOR-16 + VOR-19 classes, sites 6-7) | lib/markets/cancel-date-cascade.ts:101-103 (per-item refund = subtotal+6.5%+prorated flat only), :177-188 (order rollup: no tip/small-order-fee refund, no sessions.expire, no status guard) | Manager cancel-date cascade: (a) killing an order's last live item never refunds the order-level tip + small-order fee (the exact leak the VOR-5B/VOR-16 decisions closed at 4 other sites); (b) a pending order is flipped cancelled without expiring its live session. NOTE: this is also the code behind the park dashboard's "Cancel a Date" card. | Port the VOR-16 zero-remaining block + VOR-19 pending→sessions.expire into the rollup; `.eq('status')` guard on the order flip. Needs user nod to extend the VOR-5B decision here (same as VOR-16 did). | S-M | **fixed** (user extended VOR-5B 2026-07-16, batch 1. Rollup now: pending→sessions.expire w/ skip-if-throw [reject pattern]; flip guarded `.in('pending','paid')`; FULL fee port — tip + recomputed small fee, `${oid}-order-fees` key [full, not tip-only: this file's per-item refunds carry no small-order-fee share]. Rule A allowlist entry removed — flip now ENFORCED) |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| MGR-4 | money-path/correctness | webhooks.ts:1253-1277 (weekly booth paid-flip guarded `.eq('status','pending_payment')` but NO rowcount), :1287-1399 (paid notifications fire regardless); contrast the season handler's cancelled_conflict→ERR_WEBHOOK_014 at :1468-1477; expire-orders:2755-2762 (Phase 16 no sessions.expire — safe today ONLY because its 24h threshold equals Stripe's max session age, an undocumented invariant) | Paying a session whose weekly rental is already cancelled silently no-ops the flip, keeps the charge, still sends "booth paid" confirmations to vendor + manager, booth resellable — zero reconciliation signal. | Rowcount-check the guarded update; 0 rows + cancelled → ERR_WEBHOOK_014 logError + skip notifications; optionally sessions.expire in Phase 16 cohort (b) + pin the 24h invariant in a comment. | S | **fixed** (2026-07-16 batch 2: cancelled pre-check via the handler's existing status read → ERR_WEBHOOK_014 + return BEFORE the flip; flip gains `.select('id')` rowcount → 0 rows → ERR_WEBHOOK_014 + skip notifications [closes the read-to-flip race]. Phase 16 sessions.expire NOT added — optional half, 24h invariant stands; note kept here) |
| MGR-5 | correctness/contract-break (PRK-7 analog) | webhooks.ts:867-878 (handleAccountUpdated updates vendor_profiles only); the only markets sync = the dashboard poll stripe/status:91-98; gates read the stale column at book:148, book-season:76 | Manager Connect accounts are excluded from account.updated sync — markets.stripe_charges_enabled only updates when the manager opens their dashboard: a disabled account keeps attracting bookings that all 502 generically with no alert; a completed onboarding keeps bookings hidden until a dashboard visit. | Extend handleAccountUpdated to also UPDATE markets by stripe_account_id. | S | **fixed** (2026-07-16 batch 2: additive markets update, same column trio as the stripe/status poll [charges/payouts/onboarding_complete], scoped `.eq('stripe_account_id', account.id)` — no-op for vendor accounts) |
| MGR-6 | correctness/data-integrity (PRK-8 class) | seasons/route.ts:236-244 (close_prepay has no status precondition; unconditionally sets active/draft); the debt gate it defeats :188-201 (open_prepay blocks while any season status='ended'); make-up window keyed on 'ended' makeup-dates:116-121 | close_prepay on an ended/settled season RESURRECTS it to active/draft → bypasses the settle-before-new-season enforcement + closes the make-up window. | Per-action status precondition (close_prepay only from open/draft/active) + rowcount→409, mirroring the PRK-8 fix. | S | **fixed** (2026-07-16 batch 1: `.in('status', draft/open/active)` + rowcount→409) |
| MGR-7 | money-path/observability | book/route.ts:478-487 (Stripe-fail credit-release insert result NEVER checked, then the rental is DELETED :488-493 so Phase 16 can't recover; the mig-169 FK SET-NULLs the redeemed row = uncorrelatable); expire-orders:2781-2790 (Phase 16 release insert result discarded) | Both booth-credit release sites ignore insert errors — a failed release permanently + silently destroys the vendor's credit balance (the −redeemed row stands with no compensating +row, no log, no trail). | Check the insert error → logError at both sites; book route: insert the release BEFORE deleting the rental and skip the delete on failure. | S | **fixed** (2026-07-16 batch 1: both sites logError on release-insert failure; book route skips the rental delete on failure so the Phase 16 orphan sweep re-attempts the release with the FK intact — vendor waits ≤30 min to rebook in that rare case) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| MGR-8 | money display (PRK-10 sibling) | webhooks.ts:1299,:1367 (vendor told fees.vendorPaysCents), :1388 (manager told managerReceivesCents) — both recomputed from price_cents ignoring session.metadata.applied_credit_cents (written at payments.ts:354); season-notifications.ts:73; manager-dashboard-stats.ts:464,:561 | Applied booth credit is invisible downstream: paid notifications + the manager earnings dashboard state GROSS amounts when the actual charge/transfer was credit-reduced (the transfers themselves are correct). | Read applied_credit_cents from session metadata in both handlers; stats: join redeemed booth_credits or snapshot net at pay time. | S-M | **SPLIT 2026-07-16: notifications fixed** (batch 2 — weekly handler nets both amounts from session.metadata.applied_credit_cents; sendSeasonPaidNotifications self-sums the group's D5 redeemed rows so BOTH callers [webhook + Phase 18 reconcile] state net). **Stats half deferred** with PRK-10 (user: agreed) — same historical-earnings-display class, one decision |
| MGR-9 | correctness (POLICY — user call) | seasons/route.ts:287-305 (set_cap: no status/prepay/paid-group guard); settlement/route.ts:65-71 (activeDaysPerWeek = LIVE schedule count); settlement-math.ts:27-30 | Two manager levers retroactively shrink what's owed to already-paid vendors: raising refund_cap_days after sales, and adding schedule days mid-season (live denominator shrinks perDayBase). No cash moves in-platform (0-amount markers), but the owed figure drives the off-platform obligation + vendor notification. | Freeze/cap set_cap once prepay_open or first paid group (vendor-favor increases only); snapshot days-per-week onto the season row at creation (it already snapshots declared_market_days). | S-M | **fixed** (mig 194 applied Dev+Staging 2026-07-16, Prod pending) (USER DECISION 2026-07-16 = BOTH. (a) batch 1: set_cap blocked on ended/settled; raise blocked once prepay_open or any paid group — lowering [vendor-favor] allowed. (b) batch 3: mig 194 `market_seasons.days_per_week_snapshot` + backfill; seasons POST writes it [tolerated UPDATE, pre-migration-safe]; settlement loadContext prefers snapshot, NULL→live-count fallback) |
| MGR-10 | observability (CHK-17/PRK-11 class) | book:385 (RPC-empty 500), :475 (Stripe session-create fail); book-season:271 (Stripe fail; :272 cancel_season_group cleanup result unchecked); webhooks:1403 (booth paid-notification catch); expire-orders:2749,:2764 (Phase 16 sweep query errors) | Money-adjacent failures console.error only — invisible to error-log review. (Phase-level catches were CRN-7's scope; these route-level sites are new.) | logError swaps (ERR_CHECKOUT_002/010 patterns exist per PRK-11); check the cancel_season_group result. | S | **fixed** (2026-07-16 batches 1+2: book:385→ERR_CHECKOUT_010, book:475 + book-season Stripe-fail→ERR_CHECKOUT_002, cancel_season_group result checked, Phase 16 sweep errors→traced.fromSupabase, webhooks booth paid-notification catch→ERR_WEBHOOK_015 [code reused from PRK-11's catalog entry]) |

**Clean (finder-verified; re-verify only at fix anchors):** book_weekly_booth_atomic mig-186 3-layer + book_season_atomic + confirm_season_paid/cancel_season_group (FOR-UPDATE, status-guarded, idempotent); fee math single-sourced in pricing.ts (no hardcoded fee anywhere in the slice); credit application conserves the platform fee; redeem RPC advisory-locked; deterministic Stripe idempotency keys; manager authz uniform across all 34 gated routes + child scoping + suspended-manager lockout + intake fraud-flagging; schedules PUT is a Session-83-compliant soft-upsert (never DELETEs); optin mig-189 filter intact; booth-inventory CRUD guards; settlement math pure; season prepay O6 window enforced both ends (modulo the MGR-6 bypass); vendor-invitations caps + eligibility. Minor unfiled: weekly_booth_rentals 'completed' dormant; vendor group-cancel sends no manager notification; broadcast/schedule/makeup fan-outs per-recipient but N small + capped; intake header doc-drift (superseded 409-on-duplicate-email behavior). Cross-refs: MGR-1↔VOR-8 · MGR-3↔VOR-16/19 · MGR-4↔PRK-1/ERR_WEBHOOK_014 · MGR-5↔PRK-7 · MGR-6↔PRK-8 · MGR-8↔PRK-10 · MGR-10↔CHK-17/PRK-11.

---

## Slice 7 — Auth / RLS / multi-vertical isolation (Fable finder, 2026-07-16) — 1 finding, AUT-1 MAIN-SESSION VERIFIED same day

Coverage: auth infra (middleware.ts, lib/supabase/{server,client,anon,middleware}, lib/auth/{admin,roles,vertical-gate,admin-accounts}); in-scope routes user/{profile,delete-account,accept-agreement}, auth/{send-email,callback}, notifications/*, buyer/{location,geocode,orders/[id]/rate,orders/unrated}, vendor non-order surfaces (profile, profile-image, listing images, onboarding docs, checkins/log, tier/downgrade, downgrade-free, stripe/onboard, event-readiness, home-market, referrals, reviews), vendor-documents/signed-url, public singles (submit/support/vendor-leads/buyer-interests/errors/report/trucks/where-today), subscriptions/checkout, markets/[id]/vendors, listings, admin spot-checks (login, users/[id], fee-override); RLS sweep of ALL migrations (79/79 public tables have ENABLE RLS); USING(true) policy audit; SECURITY DEFINER/grant scan migs 153→194; all 30 vendor_profiles.update sites for the CHK-9 class. **Finder question answers: A = service-client discipline CLEAN (every non-slice-1-6 site behind auth+ownership/role gate, or public write-only lead/ticket insert, or public-read re-filtered to public-safe fields; admin helpers gate on hasAdminRole before returning the service client). B = RLS coverage CLEAN (79/79 enabled; only USING(true) policies are SELECT on genuinely public data [order_ratings/vendor_location_cache/zip_codes], writes separately scoped; NO post-152 migration reopens anon/authenticated EXECUTE on money/PII functions — all money RPCs re-assert REVOKE+GRANT service_role). C = multi-vertical CLEAN for the CHK-9 class (no remaining user_id-only writes to vendor_profiles; notifications vertical-scoped; vendor subs fail-fast without vertical). D = IDOR: ONE finding (AUT-1); all other [id] surfaces verify ownership. E = auth flows CLEAN (service key server-only; no body-controlled role/tier fields anywhere — user/profile + vendor/profile use fixed allowlists; tier only via Stripe webhooks; fee-override clamped; optional admin MFA gate). F = anon exposure + rate limiting CLEAN (auth-sensitive routes limited; public reads expose only intentional public surfaces).**

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| AUT-1 | security/data-integrity (IDOR — reputation, not money) | buyer/orders/[id]/rate/route.ts:35 (body vendor_profile_id), :68-74 (order-only checks), :110-118 (insert); mig 20260123_002:68-78 (INSERT policy constrains order, NOT vendor), :30-57 (trigger recomputes target vendor's public stats), :16 (UNIQUE caps 1/(order,target)) | **Rating IDOR / review fraud:** POST never verifies the submitted vendor_profile_id is a vendor ON the order — a buyer with any completed order can plant a 1-star rating on ANY vendor (N completed orders → N ratings on one competitor); trigger updates the victim's public average_rating/rating_count. MAIN-SESSION VERIFIED 2026-07-16 (all anchors read). | Route: verify vendor_profile_id ∈ the order's order_items vendor set before insert/update (mirror buyer/orders/unrated:78-97 which already derives the legit set client-side); optionally tighten the RLS INSERT/UPDATE policies with the order_items EXISTS (migration). | S (code) + S (mig) | **fixing — code done + mig 195 WRITTEN (user applies)** (2026-07-16, user: "do it all". Route: order_items existence check → 403 pre-insert/update, works pre-migration. Mig 195: INSERT policy + UPDATE policy [which had NO WITH CHECK — retarget hole closed too] both gain the order_items EXISTS tie; `(SELECT auth.uid())` form; SELECT/DELETE untouched) |

**Minor unfiled — slice 7 (finder-reported, NOT main-session verified):** (1) markets/[id]/vendors:143 compares vendor_profiles.user_id to user_profiles PK instead of auth uid → self-apply POST always 403s — fail-CLOSED broken feature, not a hole (real path = vendor/markets/[id]/join); (2) user/delete-account:37-51 leaves `phone` populated after anonymization + console.error-swallows sub-errors then reports success (CHK-17 observability class on a PII path); (3) order_ratings public SELECT exposes buyer_user_id UUID + comments — assessed intentional (public reviews). Cross-refs: AUT-1 ↔ VOR-3 ownership-gate class (reputation surface, not money).

---

## Slice 10 — Admin (49 routes: permissions, data accuracy) — Fable finder, 2026-07-16 — 8 findings; P1s + P2s MAIN-SESSION VERIFIED same day

Coverage: all 50 route files under admin/** (glob-complete) + shared gates lib/auth/{admin,admin-accounts}. **GATE COMPLETENESS BAR PASSED: the admin gate (hasAdminRole / vertical_admins-fallback-scoped-to-target-vertical / verifyAdminScope) is present and blocking BEFORE any privileged read/write on 49/49 gated routes — ZERO ungated, zero gate-after-side-effect. 50th file = login (the auth endpoint). One deliberate exception: errors/[id] enforces via error_reports RLS (mig 015:110-155, verified) not an app gate — fail-closed.** Role model (main-session-confirmed): platform admin = user_profiles.role/roles contains 'admin'; vertical admin = a vertical_admins row only (grant never writes user_profiles.role) → every hasAdminRole gate is platform-only; vertical-admin access exists only via explicit vertical_admins fallback or verifyAdminScope. **Finder question answers: A = gate uniform (3 sound patterns; the 2 scope bugs ADM-1/ADM-4 are in the verifyAdminScope callers that use the raw body vertical instead of scope.effectiveVerticalId). B = destructive ops all gated + input-validated (operator_keep_pct clamp, action enums, tier=fixed trial constants, admin self/last-chief guards); the one blast-radius gap = market hard-DELETE (ADM-3); NO admin route moves Stripe money directly. C = analytics overview/trends/top-vendors consistent + MB total_paid_cents pre-fee post-MBX-1 (clean); the REPORTS accounting suite misuses platform_fee_cents (ADM-2). D = [id] routes vertical-scope correctly (clean IDOR); ADM-1/ADM-4 are scope-plumbing. E = no money-mutation swallows errors (admin CRUD 500s console.error-only = P3 class, not filed). F = enums validated at mutations; a few unvalidated filter params (quality-checks ?vertical amplifies ADM-5).**

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| ADM-1 | security (cross-tenant scope) | admin/reports/route.ts:60 (raw body verticalId), :63-69 (scope check), :78 + :86-102 (filename + all 21 generators take body `verticalId`, NOT scope.effectiveVerticalId); lib/auth/admin.ts:244-251 (single-vertical no-arg auto-authorize) | A vertical admin with exactly one vertical who omits verticalId is authorized by the admin.ts fallback, but every report generator filters on the raw body verticalId (undefined) → all 21 reports generate PLATFORM-WIDE: cross-vertical revenue, vendor payouts, vendor roster w/ emails+phones (:885-886), customer names+emails (:978-998). Sibling routes (error-logs:54, feedback:53, event-ratings, quality-checks) correctly use scope.effectiveVerticalId — reports is the lone deviation. VERIFIED 2026-07-16. | `const effectiveVertical = scope.effectiveVerticalId ?? verticalId`; pass into all generators + filename prefix. | S | **fixed** (2026-07-16 batch A: body verticalId renamed requestedVerticalId; `const verticalId = scope.effectiveVerticalId ?? requestedVerticalId` after the scope check → all 21 generators + filename inherit the scoped value; platform admin [effectiveVerticalId=null] keeps unscoped-all) |
| ADM-2 | data-accuracy/money-display (CHK-6/CHK-15/MGR-8/EVT-11 fee-semantics family, worst instance) | admin/reports/route.ts:1367-1370 (transaction_reconciliation), :1710/:1727 (tax_summary), :335/:342-344 (revenue_fees), :1804-1821 (monthly_pnl); ROOT: checkout/session/route.ts:569 (`platform_fee_cents = round(subtotal×(buyerFeePercent+effectiveVendorFeePercent)/100)` — BOTH sides, ~13%, no flats) | Accounting reports treat order_items.platform_fee_cents (combined ~13%) as the buyer-side 6.5% fee. transaction_reconciliation (bank-recon CSV): "Buyer Fee"=13% (2× over), "Buyer Paid"=subtotal+13% ≠ actual Stripe charge (subtotal×1.065 + flat share + small fee + tip), "Platform Revenue"=subtotal−payout = vendor-side only ≈ HALF actual. $10 item: report says buyer $11.30/platform $0.80; actual $10.80/$1.60 — won't tie to Stripe. tax_summary/revenue_fees/monthly_pnl omit flats + small-order + tip-platform portions; monthly_pnl estimates Stripe cost on base not charge + $0.30/ITEM not /order. Reporting-only (transfers correct). ROOT VERIFIED 2026-07-16 (session:569). | Shared per-item fee decomposition helper (buyer-side vs vendor-side from platform_fee_cents + pricing.ts; allocate order-level flats/small-fee/tip once per order); "Buyer Paid" must reconstruct the real charge. | M | **fixed** (2026-07-17 batch B, USER DECISIONS: order-level reconcile + net-headline. NEW pure helper `lib/reports/platform-revenue.ts` [9 unit tests incl. multi-vendor]: platform_gross = total_cents − Σvendor_payout − vendorTipShare; net = gross − estStripe(2.9%+30¢/charge, external=0) − Σrefunds. Order-level amounts counted ONCE per order [multi-vendor safe]; per-item refunds exact. Wired into transaction_reconciliation [Buyer Paid/gross/stripe/net once per order, per-item cols per row], revenue_fees, monthly_pnl [full net P&L]; tax_summary fee col relabeled honestly [per-state net ill-defined for multi-state orders — gross-by-state kept]. gates tsc0/vitest1685. AWAITING USER before/after sign-off pre-commit) |
| ADM-3 | data-integrity/blast-radius (Session-83 schema-intent class) | admin/markets/[id]/route.ts:314-330 (guard checks ONLY listing_markets), :332-336 (hard `.delete()` on markets); 22 tables `REFERENCES markets(id) ON DELETE CASCADE` (grep-verified: weekly_booth_rentals, park_spot_bookings, booth_credits, booth_booking_groups, season tables, standing_reservations, checkins, market_date_overrides, docs, …) | Admin market DELETE hard-deletes with only a "has listings?" guard. A market with 0 listings but PAID financial history (booth rentals, park bookings, booth-credit ledger, season/settlement rows) cascades ALL of it away silently — financial audit trail destroyed, Phase 16/18 cron reconciliation loses rows. `markets.active`/`status` exist as the designed soft-delete signals. VERIFIED 2026-07-16 (guard + 22-table cascade count). | (a) soft-delete (status/active flip, matches schema intent), OR (b) extend the guard to block when any paid booth/park/credit/group rows exist, naming what blocks. | S-M | **fixed** (USER DECISION 2026-07-16 = guard. Batch A: parallel existence checks on weekly_booth_rentals / park_spot_bookings / booth_booking_groups / booth_credits by market_id → 400 "deactivate instead" if any exist, before the hard delete) |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| ADM-4 | security (scope / stated-contract break) | admin/event-ratings/route.ts:132-135 (verifyAdminScope(null) → checks only scope.authorized), :137-138 (comment: "PATCH is platform-admin only… vertical admins cannot moderate" — but NO isPlatformAdmin check follows), :173-177 (update by bare id, no vertical scoping) | Code doesn't enforce its own stated contract: a single-vertical vertical admin is authorized by the admin.ts:244-251 fallback, no isPlatformAdmin gate follows, update targets by bare id → a vertical admin can approve/hide ANY vertical's public event ratings. VERIFIED 2026-07-16. | Add `if (!scope.isPlatformAdmin) return 403` after the gate (matches the comment), or scope the update to the rating's event vertical. | S | **fixed** (2026-07-16 batch A: `if (!scope.isPlatformAdmin) return 403` after the authorized check — code now matches its own comment) |
| ADM-5 | data-integrity (MBX-3 unscoped-UPDATE + CRN-14 ordering class) | admin/quality-checks/route.ts:158-161 (supersede ALL active findings — `.eq('status','active')` only, NO vertical filter) vs :171-176 (re-create only for effectiveVertical); :119 (vertical param unvalidated) | Manual quality-scan supersedes EVERY active finding platform-wide then re-creates only the requested vertical's → a vertical admin's scan (or ?vertical=bogus) silently+permanently empties the OTHER vertical's findings dashboard. Also inherits CRN-14 supersede-before-insert (crash mid-scan = all findings lost). VERIFIED 2026-07-16 (unscoped supersede vs scoped recreate). | Scope supersede `.eq('vertical_id', effectiveVertical)` when set; validate vertical against `verticals`; supersede AFTER successful insert (CRN-14 pattern). | S | **fixed (scope half)** (2026-07-16 batch A: supersede scoped `.eq('vertical_id', effectiveVertical)` when the scan is vertical-scoped — stops the cross-vertical wipe AND makes a bogus ?vertical= a safe no-op; platform-admin full scan still supersedes all. Explicit vertical-validation query skipped [scoping already makes bogus safe]. Supersede-before-insert crash-window = folded into CRN-14, not this fix) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| ADM-6 | correctness (pricing hardcode — KNOWN_AND_OUT_OF_SCOPE mandates flagging hardcoded fees) | admin/reports/route.ts:1647,:1662 vs pricing.ts:23-45 (SUBSCRIPTION_AMOUNTS) | subscription_revenue report hardcodes $25/$50/$9.99 instead of SUBSCRIPTION_AMOUNTS + reports monthly rate for annual-cycle subs. Finder-reported (anchor not main-session re-read). | Derive from SUBSCRIPTION_AMOUNTS + subscription_cycle. | S | **fixed** (2026-07-17 batch B: `effectiveMonthlyCents()` helper derives from SUBSCRIPTION_AMOUNTS; annual cycles divided /12 to a monthly-equivalent instead of reporting the monthly rate) |
| ADM-7 | correctness/ux | admin/listings/[id]/route.ts:105-113 (unsuspend → status:'published' unconditional), :74-82 (suspend stores no prior status) | Suspend→unsuspend force-publishes regardless of pre-suspension state (draft/sold_out → live). Finder-reported. | Store prior status at suspend + restore, or restrict unsuspend to previously-published. | S | **fixed** (2026-07-16 batch A: suspend stashes listing_data.status_before_suspension [only when prior status ≠ 'paused']; unsuspend restores it + clears the key, defaulting to 'published' for legacy pre-fix rows. No migration — listing_data JSONB already exists) |
| ADM-8 | data-integrity (audit trail) | admin/vendors/[id]/verify/route.ts:71-80 (`notes: notes\|\|null` overwrite) vs fast-track:137-143 (append) | Gate-1 verify REPLACES vendor_verifications.notes — a re-verify without notes nulls the prior review trail; fast-track deliberately appends. Finder-reported. | Append with a marker like fast-track; never null over existing notes. | S | **fixed** (2026-07-16 batch A: notes omitted from the update unless the reviewer wrote some; when provided, appended with a `[action YYYY-MM-DD]` marker to the prior notes — never overwrites/nulls the trail) |

---

## Slice 9 — Notifications (single pipe, fan-out, email volume) — Fable finder, 2026-07-17 — 5 findings; all MAIN-SESSION VERIFIED same day

Coverage: notifications/service.ts (full pipe — sendNotification, sendNotificationBatch, in-pipe dedup, channel dispatchers), types.ts (registry + URGENCY_CHANNELS + getNotificationUrgency), webhooks/resend route, fan-out sites (manager broadcast/schedules, season-notifications). **Finder question answers: A2 channel independence CLEAN (each dispatcher try/caught → ChannelResult, no half-sent rollback harm). C no NEW duplicate-send path (CHK-10 + CRN-13 already filed; webhook resends guarded by wasNotificationSent [CHK-13] + the 10s in-pipe dedup). D vertical+email resolution in pipe CLEAN (email from user_profiles.email only if caller omits; vertical via OPTIONS). E template integrity CLEAN (unknown type → error ChannelResult not throw; callers type-checked; money fields all $-formatted, no raw-cents leak). Pre-existing notification findings all still valid; NEW evidence attached to CHK-13 [→NOT-1, service-level dedup has the same defect the webhook helper got fixed for] and CRN-11/12/EVT-16/PRK-13 [→NOT-2, their recommended sendNotificationBatch target doesn't reduce per-recipient queries].**

### P1
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| NOT-1 | correctness | service.ts:416-438 (dedup keys user_id+type, 10s window, NO dedupRef) | In-pipe dedup suppresses ANY same-type notification to one user within 10s regardless of reference → two orders to the same vendor within 10s (lunch rush) drop the 2nd `new_paid_order` entirely (no in-app row, no push); cron expiring 2 of one buyer's orders in a run notifies about one. CHK-13 fixed this exact defect in the webhook helper (wasNotificationSent) but NOT here. MAIN-SESSION VERIFIED 2026-07-17 (10s window bounds blast radius but real at volume). | When templateData carries a dedupRef, add `.contains('data',{dedupRef})` to the dedup query (mirror webhooks.ts:36); fall back to the coarse user+type window only when no dedupRef. | S | **fixed** (2026-07-17: dedup now reference-aware — matches on `dedupRef` (CHK-13 sends) OR `orderNumber` (order notifications) via `.contains('data',{...})`; coarse user+type+10s window applies only when NEITHER is present. Two distinct orders to one vendor in a lunch rush no longer cross-suppress) |

### P2
| ID | Cat | Anchor | Claim | Fix | Effort | Cost note | Status |
|---|---|---|---|---|---|---|---|
| NOT-2 | efficiency | service.ts:618-657 (batch prefetches email/phone only) vs :459-499 (per-recipient profile+vendor queries still run) | `sendNotificationBatch` prefetches only email/phone; each inner sendNotification still runs its own dedup query + `user_profiles.select('*')` (:461) + optional `vendor_profiles` (:487) per recipient. The ":616 avoid N sequential calls" comment is misleading — reads are parallelized, not reduced. So the ledger's CRN-11/12/EVT-16/PRK-13 fixes (which say "switch to sendNotificationBatch") recover parallelism only, NOT query count. VERIFIED 2026-07-17. | Batch also prefetch notification_preferences + tier, thread into sendNotification via optional params so the per-recipient profile/vendor queries are skippable; or add a true bulk-insert path. | M | N recipients → ~2N-3N reads + N emails (parallel) now → ~2-3 bulk reads + N emails after. Multiplies against every fan-out caller. | **fixed** (2026-07-17: sendNotification gained an optional `prefetched` bundle {preferences,email,phone,vendorTier}; when present it SKIPS both per-recipient queries. sendNotificationBatch now bulk-loads full profiles + vendor tiers in exactly 2 queries and threads them in — N×~2 reads → 2. NB the batch had ZERO callers before — built-but-unwired; now wired at CRN-12 + EVT-16. Consumers switched: see below) |

### P3
| ID | Cat | Anchor | Claim | Fix | Effort | Status |
|---|---|---|---|---|---|---|
| NOT-3 | correctness/ux | types.ts:27-32 (URGENCY_CHANNELS.info=['email'] — NO in_app); info types order_fulfilled:385, inventory_low_stock:1243, payout_processed:1261 | `info`-urgency types dispatch EMAIL ONLY — no in_app bell row ever written; a recipient who set email off (or has no email / Resend down) gets nothing. Most impactful: `payout_processed` (vendor money event — VERIFIED urgency='info' :1261) + `order_fulfilled` (buyer). MAIN-SESSION VERIFIED 2026-07-17. | Add `in_app` to the info channel set (`['email','in_app']`) for a durable trail; or promote payout_processed/order_fulfilled to standard. | S | **BLOCKED — test-integrity decision point (2026-07-17):** "info = email only" is a LOCKED design (notification-types.test.ts:394-396 assertion + :97 explicit carve-out comment "info is email-only by design"). NOT-3's fix contradicts it. Attempted `info+=in_app` → test failed → REVERTED (never change a business-rule test to match code). USER DECISION NEEDED: (a) change the design [info += in_app, updates 2 business-rule tests — needs explicit test-baseline approval] OR (b) promote the 3 info types (payout_processed/order_fulfilled/inventory_low_stock) to 'standard' [gives them a bell row, leaves the info design + tests intact, but changes their urgency classification]. **RESOLVED 2026-07-17 via COMM-3, neither (a) nor (b): the frugal answer was `info = ['in_app']` (free bell row, drop the paid email) — the finder's "add in_app [keep email]" was backwards for cost. `info=['in_app']`, tests updated to new spec. NOT-3's "no durable trail" concern fixed; the email cost also removed.** |
| NOT-4 | correctness | service.ts:446-449 (actionUrl), :505-506 (title/message), :602 (logError) — all OUTSIDE any try/catch | The "sendNotification never throws" contract (money-path callers await it un-try/caught) has unguarded sections: a throwing template fn or a throwing logError rejects the promise. Real risk low (templates use `||''` fallbacks) but not structurally guaranteed. VERIFIED 2026-07-17 (dedup + profile fetch ARE try/caught; these three sites are not). | Wrap title/message/actionUrl generation + the final logError in try/catch (generic fallback / swallow) so the contract holds structurally. | S | **fixed** (2026-07-17: actionUrl → try/catch fallback ''; title/message → try/catch fallback 'Notification'/''; final logError → try/catch + console last-resort. Contract now structurally holds) |
| NOT-5 | efficiency/observability | webhooks/resend/route.ts (writes bounces/complaints to email_events) — `email_events` has ZERO readers (grep) | Bounce/complaint events stored but never read: no pre-send suppression, so hard-bounced addresses keep receiving sendNotification emails (wasted Resend calls + sender-reputation risk); only Resend's own list suppresses complaints. Finder-reported (grep-based). | Pre-send suppression check against email_events (hard bounce/complaint on the address, cached) or a `suppressed` profile flag the pipe honors. | M | open (unverified) |

---

## Communication-COST review (Fable finder, 2026-07-17) — owner frugality directive; 8 findings COMM-1..8, key anchors MAIN-SESSION VERIFIED

Lens: paid comms (email=Resend/send, SMS=Twilio/most-$, cron=compute+always-on) are negligible when small but scale to real dollars; prefer FREE in_app + next-page-load when the user can wait. **Channel econ (types.ts:27-32 VERIFIED): immediate=push+in_app (~free), urgent=sms+in_app (most $, but SMS opt-in default-off [service.ts:99] + boss-tier [vendor-limits.ts:94]), standard=email+in_app (the volume email tier, no tier trim for buyers), info=email-ONLY (pays email, skips free in_app — inverse of frugal).** SMS well-contained EXCEPT the isCritical tier-bypass (COMM-7). Principle recs recorded in memory `feedback_communication_cost_frugality`.

### Large savings
| ID | Lens | Anchor | Current cost | Proposed frugal design | UX risk | Effort | Status |
|---|---|---|---|---|---|---|---|
| COMM-1 | channel/defer | types.ts:1105 (market_day_today urgency='standard'); surveys/route.ts:213-224 (per-follower send) | **"Market open today" = email+in_app to EVERY follower EVERY operating day** — linear in followers×market-days; the single largest recurring automated email cost. VERIFIED 2026-07-17. | Reclassify to `immediate` (push+in_app) — drops the email; push is exactly a "come shop" nudge, in_app bell lands on next open. Email → explicit opt-in only. | Low (follower w/o push who doesn't open that day misses a low-stakes ping; not a transactional record). Note push default-off (service.ts:100) → most get in_app-only, still free+durable. | S | **fixed** (USER DECISION 2026-07-17: market_day_today urgency 'standard'→'immediate' = push+in_app, drops the per-follower-per-day email — the single largest recurring automated email cost) |
| COMM-2 | fan-out/dup | surveys/route.ts:441-451 (sendNotification standard=email) + :467-492 (sendSurveyEmail) | **TWO emails per survey recipient** — the branded standard email AND the custom-logo email. Doubles survey Resend spend (on top of CRN-11's query N+1). VERIFIED 2026-07-17. | Make survey_request_* in_app-only (per-type channel override), keep the richer sendSurveyEmail as the single email → HALVES survey email volume. | None (still one nicer email + in_app bell). | S-M | **fixed** (2026-07-17: survey_request_vendor/buyer urgency → 'info' = in_app-only [after COMM-3]; sendSurveyEmail is now the SINGLE email. Delivered together with COMM-5) |
| COMM-3 | channel (NOT-3 cost lens) | types.ts:31 (info=['email']); order_fulfilled:385, inventory_low_stock:1243, payout_processed:1261 | info types pay email + write NO free in_app row — worst of both (recurring email $ + no durable bell). **Cost INVERSE of NOT-3** (which argues add in_app, keeping email). | (a) non-money info (inventory_low_stock, order_fulfilled) → in_app-ONLY (drop email, deferred-to-nav); (b) payout_processed (money) → email+in_app. Per-type. **Resolves the NOT-3 decision in the frugal direction.** | Low for (a) [status/FYI]; (b) keeps the money record. | S | **fixed** (USER DECISION 2026-07-17: `info` → in_app-ONLY for ALL three [incl. payout_processed — Stripe notifies separately + in_app bell = durable record]. `URGENCY_CHANNELS.info=['in_app']`. **RESOLVES NOT-3.** Business-rule tests updated to new spec — see below) |

### Medium
| ID | Lens | Anchor | Current cost | Proposed frugal design | UX risk | Effort | Status |
|---|---|---|---|---|---|---|---|
| COMM-4 | cron timing | vercel.json:16-19 (surveys=`0 * * * *`) vs the windowed park-docs-review `0 12-23,0-2 * * *`; surveys morning-guard route:173 | Surveys cron runs **hourly 24/7** = 24 runs/day, each scanning all markets+parks; sends only at local-morning/close fire-moments → ~8 dead-hour no-op runs/day (UTC ~03:00-11:00, no US market activity). Owner principle #3 violation. | Window the schedule to a business-hours UTC band mirroring park-docs-review (already ~06:00-20:00 CT + the 0-2 band for PT). Kills dead-hour invocations. | None if the window spans every US-tz fire moment (park-docs window already does; verify HI/AK if in scope). | S | **PART 1 DONE (2026-07-17): daily-gate.** Survey generation `runSurveyCron()` now runs ONCE/day (gated to 15:00 UTC — past every US 08:00-local fire moment) instead of 24×/hour; the route stays hourly for the intraday park-checkin + market-day reminders that share it; per-market fire-window + per-row UNIQUE keep it correct + idempotent. Keeps the single email (COMM-2) + free in_app. Constraint that forced this over a pure event-trigger: the cron route is SHARED with jobs needing hourly timing. **PART 2 DONE (2026-07-17): LAZY on-return in-app generation.** New `lib/surveys/lazy-generate.ts ensurePendingVendorSurveys()` — when a vendor lands on their dashboard (PendingSurveysCard render), it creates any DUE survey row (past fire moment, no row) + in_app, NO email. Best-effort (never breaks the render). The daily cron's existing 23505-skip means a returner surfaced this way is automatically NOT emailed (cron emails only rows IT creates = non-returners). `computeExpiresAt` moved to cron-helpers so cron + lazy share it (no drift). Idempotent via market_surveys UNIQUE. **Buyer lazy path DONE (2026-07-17):** `ensurePendingBuyerSurveys()` (recent fulfilled/completed pickups → due survey rows w/ access_token + in_app, no email) hooked into the /buyer/surveys read path (buyers have no passive dashboard survey card; the list page is their read path). COMM-4 FULLY COMPLETE (both parts, both audiences). |
| COMM-8 | fan-out/digest | types.ts:1632 (FM new_paid_order override → standard=email); per-order at checkout/webhook | Busy FM Saturday = one email per order to a vendor (8 orders/hr = 8 emails). | Digest a short window of new_paid_order per vendor into one "N new orders" email; OR move FM to immediate (push+in_app) like FT. | Medium — must not delay vendor order-awareness enough to hurt prep; push swap safer than batched-email delay. | S | **fixed** (USER DECISION 2026-07-17: FM new_paid_order → immediate [push+in_app], dropping the per-order email. NI-R22 override + test updated) |

### Small
| ID | Lens | Anchor | Current cost | Proposed frugal design | UX risk | Effort | Status |
|---|---|---|---|---|---|---|---|
| COMM-5 | opt-out leak | surveys/route.ts:574 (opt-out gates only sendSurveyEmail) vs :441-451 (sendNotification email fires regardless) | A buyer who opted OUT of survey emails still gets the standard branded survey email (opt-out only suppresses the custom email). Cost leak + half-honored preference. | Auto-fixed by COMM-2 (in_app-only survey type = no sendNotification email); else thread the opt-out into the survey sendNotification. | None (honors a stated preference). | S | **fixed** (2026-07-17: survey → in_app-only [COMM-2] so the only email is the opt-out-gated sendSurveyEmail — opt-out now honored) |
| COMM-6 | channel | types.ts:413-414 (order_refunded urgency='urgent' → SMS-eligible) | A refund notice is informational-not-actionable yet provisioned for the most-expensive channel when buyer has SMS on. | Downgrade to standard/info; reserve SMS for actionable-now. | Low (refund confirm not time-critical to the minute). | S | **fixed** (USER DECISION 2026-07-17: order_refunded urgency 'urgent'→'standard' = email+in_app, no SMS. NI-R31 test updated) |
| COMM-7 | tier leak | service.ts:485-499 (isCritical bypasses tier gating) vs vendor-limits.ts:94 (SMS=boss-only) | The critical bypass means the boss-only SMS cap doesn't hold for urgent types — a free/basic vendor who opted into SMS gets SMS on payout_failed/subscription_expired/etc. Small $ now (opt-in default-off) but the tier promise leaks. VERIFIED 2026-07-17. | If SMS is a boss perk, apply the tier filter to SMS even on urgent (keep push/in_app bypassed). | Low. | S | **wontfix (USER DECISION 2026-07-17: "if it's critical it's ok to bypass tiers" — the isCritical SMS bypass is intended; a genuinely urgent event reaches the vendor regardless of tier).** |

**Design principles (→ memory feedback_communication_cost_frugality):** (1) channel by time-sensitivity — actionable-now→push/in_app, FYI→in_app-only, email for off-platform records, SMS only urgent+actionable; (2) in_app-only default for FYI, email opt-in for recurring broad nudges; (3) one event=one paid send (never sendNotification-email + a 2nd Resend for the same event unless the type is in_app-only); (4) cron schedules follow market hours not the clock; (5) fan-out→digest before fan-out→N sends; (6) page-load carries deferrable data, don't push it. Cross-refs: COMM-3↔NOT-3 (resolves it, frugal direction) · COMM-2/4↔CRN-11/12/13 (email-$ + cron-timing halves of the surveys cron; CRN covered query count) · COMM-8↔NOT-2 (digest needs a real bulk path, not just sendNotificationBatch) · COMM-5↔survey opt-out · EVT-16/PRK-13 generalized by principle #5.

---

**Minor unfiled — slice 9 (finder-reported):** manager broadcast:168-179 / schedules:407-417 / season-notifications fan out via `Promise.all(map(sendNotification))` — already parallel + bounded recipient counts, low priority, but inherit NOT-2's per-recipient query cost (fold into NOT-2's fix). Cross-refs: NOT-1↔CHK-13 (same defect, webhook helper fixed / service dedup not) · NOT-2↔CRN-11/CRN-12/EVT-16/PRK-13 (their batch target doesn't cut queries as written) · NOT-3↔CHK-10 (payout_processed also in the webhook dedup family) · NOT-5 net-new.

---

**Minor unfiled — slice 10 (finder-reported):** (1) `isPlatformAdminCheck` (admin.ts:104-118) is DEAD + checks only 'platform_admin' which no grant writes; `hasPlatformAdminRole` includes 'admin' making the errors:60 "H-7" distinction vacuous — harmless today but the first vertical-admin given role='admin' silently becomes platform admin. (2) REQUIRED_ADMIN_EMAILS not enforced at the revoke route (only self/chief guards; total lockout impossible so not filed). (3) existence-oracle: service-client single-row fetch before the gate on several [id] routes (404-vs-403, 1-bit leak). (4) admin CRUD 500s console.error-only (CHK-17/PRK-11/MGR-10 class, but no money moves — P3). (5) efficiency (admin-click-scale, no cron amplification): top-vendors + customer_retention full-table fetches. Cross-refs: ADM-2↔CHK-6/CHK-15/MGR-8/EVT-11 fee-semantics family · ADM-3↔MBX-3/Session-83 schema-intent · ADM-5↔CRN-14+MBX-3 · ADM-1/ADM-4↔slice-7 AUT (its admin spot-checks confirmed the gate helpers; these are caller-side scope bugs the spot-check didn't reach).
